# `inbox/` — captura rápida

Joga aqui o que ainda não tem lugar: link, ideia, print de conversa, pendência solta.
Processar depois — mover pra `subjects/`, `memory/inputs/` ou virar card.

Padrão de nome: `<YYYY-MM-DD>-<assunto>.md`
