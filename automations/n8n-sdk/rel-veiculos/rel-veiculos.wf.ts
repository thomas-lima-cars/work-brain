import { workflow, node, trigger, newCredential, expr } from '@n8n/workflow-sdk';

const rodar = trigger({
  type: 'n8n-nodes-base.manualTrigger',
  version: 1,
  config: { name: 'Rodar', position: [0, 0] }
});

const fase1 = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: {
    name: 'Montar Fase 1',
    position: [224, 0],
    parameters: {
      mode: 'runOnceForAllItems',
      language: 'javaScript',
      jsCode: "/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   N\u00d3 \"Montar Fase 1\" \u2014 dimensiona antes de coletar\n\n   Relat\u00f3rio de ve\u00edculos em evento, com as lojas mais aderentes a cada um.\n\n   \u2500\u2500\u2500 COMO ESCOLHER OS EVENTOS \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n   Duas formas, e s\u00f3 uma vale por vez:\n\n     EVENTOS_IDS = [23860, 23861]   -> analisa exatamente esses\n     EVENTOS_IDS = []               -> usa a regra das pr\u00f3ximas N horas\n\n   O padr\u00e3o \u00e9 a regra de 48h porque foi o recorte pedido em 2026-09-09.\n   Para uma edi\u00e7\u00e3o espec\u00edfica, \u00e9 s\u00f3 preencher a lista.\n\n   \u2500\u2500\u2500 POR QUE UMA FASE S\u00d3 PRA CONTAR \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n   O `OFFSET` faz o banco re-executar a query inteira a cada p\u00e1gina, ent\u00e3o\n   p\u00e1gina que sobra custa um agregado completo e devolve zero linha. Na\n   execu\u00e7\u00e3o 49803 isso queimou 170 chamadas. Contando antes, a fase 2\n   monta exatamente ceil(n / 50) p\u00e1ginas.\n\n   Limites do MCP j\u00e1 medidos (automations/n8n-sdk/README.md):\n   sem fun\u00e7\u00e3o de janela, resposta cortada em 50 linhas, deadline de 60s.\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\nconst EVENTOS_IDS = [];      /* vazio = usa a regra de horas abaixo */\nconst HORAS_ADIANTE = 48;\nconst MESES_HISTORICO = 6;   /* janela do perfil de compra das lojas */\nconst PAGE = 50;             /* teto duro do MCP. N\u00c3O aumentar */\nconst LADO = 'buyer_shop_id';  /* a loja que DEU o lance */\n\nconst now = new Date();\nconst pad = (n) => String(n).padStart(2, '0');\nconst ini = new Date(now.getFullYear(), now.getMonth() - MESES_HISTORICO, now.getDate());\nconst DATA_INI = ini.getFullYear() + '-' + pad(ini.getMonth() + 1) + '-' + pad(ini.getDate());\n\n/* o filtro de evento, montado uma vez e reusado na fase 2 */\nconst SELECAO = EVENTOS_IDS.length\n  ? \" e.deleted_at IS NULL AND e.id IN (\" + EVENTOS_IDS.join(',') + \")\"\n  : \" e.deleted_at IS NULL AND e.status = 1\" +\n    \" AND e.finish_date_event >= NOW()\" +\n    \" AND e.finish_date_event <= DATE_ADD(NOW(), INTERVAL \" + HORAS_ADIANTE + \" HOUR)\";\n\n/* negocia\u00e7\u00e3o dispon\u00edvel. status 1 = em aberto; 2, 3 e 7 = vendido.\n   Medido na sonda a6fNNTUYYayehNIn, execu\u00e7\u00e3o 49813 \u2014 n\u00e3o \u00e9 suposi\u00e7\u00e3o. */\nconst DISPONIVEL = \" an.deleted_at IS NULL AND an.status = 1\";\n\nconst JANELA_OFERTAS =\n  \" o.deleted_at IS NULL AND o.\" + LADO + \" IS NOT NULL\" +\n  \" AND o.created_at >= '\" + DATA_INI + \"' AND o.price > 0\";\n\nconst Q = [];\nfunction push(nome, sql) {\n  Q.push({ queryName: nome, database: 'cars2you_production', sql: sql + ' LIMIT ' + PAGE + ' OFFSET 0', pagina: 0 });\n}\n\n/* quais eventos o recorte pegou */\npush('q_eventos',\n  \"SELECT e.id AS evento_id, e.name AS evento, e.status AS ev_status,\" +\n  \" DATE_FORMAT(e.start_date_display, '%Y-%m-%d %H:%i') AS ini_display,\" +\n  \" DATE_FORMAT(e.finish_date_event, '%Y-%m-%d %H:%i') AS fim_evento\" +\n  \" FROM events e WHERE\" + SELECAO + \" ORDER BY e.finish_date_event\");\n\n/* quantos ve\u00edculos dispon\u00edveis \u2014 dimensiona a pagina\u00e7\u00e3o da fase 2 */\npush('q_veic_total',\n  \"SELECT COUNT(DISTINCT a.vehicle_id) AS veiculos,\" +\n  \" COUNT(DISTINCT an.id) AS negociacoes,\" +\n  \" COUNT(DISTINCT e.id) AS eventos\" +\n  \" FROM advertisement_negotiations an\" +\n  \" INNER JOIN events e ON e.id = an.event_id AND\" + SELECAO +\n  \" INNER JOIN advertisements a ON a.id = an.advertisement_id AND a.deleted_at IS NULL\" +\n  \" INNER JOIN vehicles v ON v.id = a.vehicle_id AND v.deleted_at IS NULL\" +\n  \" WHERE\" + DISPONIVEL);\n\n/* quantas lojas t\u00eam hist\u00f3rico de oferta \u2014 dimensiona o perfil */\npush('q_lojas_total',\n  \"SELECT COUNT(DISTINCT o.\" + LADO + \") AS lojas, COUNT(*) AS ofertas\" +\n  \" FROM offers o WHERE\" + JANELA_OFERTAS);\n\n/* o value_actual est\u00e1 preenchido? O Thomas escolheu ele como pre\u00e7o do\n   ve\u00edculo; se vier nulo ou zero na maioria, o relat\u00f3rio nasce cego. */\npush('q_valor',\n  \"SELECT COUNT(*) AS negociacoes,\" +\n  \" SUM(CASE WHEN an.value_actual IS NULL THEN 1 ELSE 0 END) AS sem_valor,\" +\n  \" SUM(CASE WHEN an.value_actual = 0 THEN 1 ELSE 0 END) AS valor_zero,\" +\n  \" ROUND(MIN(NULLIF(an.value_actual, 0)), 2) AS minimo,\" +\n  \" ROUND(AVG(NULLIF(an.value_actual, 0)), 2) AS media,\" +\n  \" ROUND(MAX(an.value_actual), 2) AS maximo,\" +\n  \" ROUND(AVG(NULLIF(an.initial_price_reference, 0)), 2) AS media_inicial,\" +\n  \" ROUND(AVG(NULLIF(a.fipe_price, 0)), 2) AS media_fipe\" +\n  \" FROM advertisement_negotiations an\" +\n  \" INNER JOIN events e ON e.id = an.event_id AND\" + SELECAO +\n  \" INNER JOIN advertisements a ON a.id = an.advertisement_id AND a.deleted_at IS NULL\" +\n  \" WHERE\" + DISPONIVEL);\n\nif (PAGE > 50) throw new Error('PAGE > 50: o MCP corta a resposta em 50 linhas');\n\nconst META = {\n  selecao: SELECAO,\n  disponivel: DISPONIVEL,\n  eventos_ids: EVENTOS_IDS,\n  horas_adiante: EVENTOS_IDS.length ? null : HORAS_ADIANTE,\n  meses_historico: MESES_HISTORICO,\n  data_ini: DATA_INI,\n  page: PAGE,\n  lado: LADO\n};\n\nreturn Q.map((q, i) => ({\n  json: { queryName: q.queryName, database: q.database, sql: q.sql, pagina: q.pagina, idx: i, total: Q.length, meta: META }\n}));\n"
    }
  }
});

const mcp1 = node({
  type: '@n8n/n8n-nodes-langchain.mcpClient',
  version: 1.1,
  config: {
    name: 'MCP Fase 1',
    position: [448, 0],
    onError: 'continueRegularOutput',
    parameters: {
      serverTransport: 'sse',
      endpointUrl: 'https://mcp-cars2you-readonly.cars2you.com.br/sse',
      authentication: 'bearerAuth',
      tool: { __rl: true, mode: 'id', value: 'run_query', cachedResultName: 'run_query' },
      inputMode: 'json',
      jsonInput: expr('{{ JSON.stringify({ sql: $json.sql, database: $json.database }) }}'),
      options: { timeout: 60000 }
    },
    credentials: { httpBearerAuth: newCredential('MCP Cars2You Readonly - BD SQL', 'Cc8CxzVDwvA3EysZ') }
  }
});

const fase2 = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: {
    name: 'Montar Fase 2',
    position: [672, 0],
    parameters: {
      mode: 'runOnceForAllItems',
      language: 'javaScript',
      jsCode: "/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   N\u00d3 \"Montar Fase 2\" \u2014 coleta dimensionada\n\n   L\u00ea da fase 1 quantos ve\u00edculos e quantas lojas existem, e monta\n   exatamente ceil(n / 50) p\u00e1ginas para cada conjunto. Nada de teto\n   chutado: p\u00e1gina que sobra custa um agregado completo e devolve zero.\n\n   Dois conjuntos:\n\n     A) os VE\u00cdCULOS do evento \u2014 uma linha por negocia\u00e7\u00e3o dispon\u00edvel, com\n        valor (`value_actual`), modelo, categoria, ano, km e a loja\n        vendedora com UF.\n\n     B) o PERFIL DE COMPRA das lojas nos \u00faltimos 6 meses \u2014 as mesmas 5\n        queries do relat\u00f3rio `LZL3mxfbMIz4avyx`, j\u00e1 provadas em produ\u00e7\u00e3o\n        (execu\u00e7\u00e3o 49803, 1.300 lojas). Sem fun\u00e7\u00e3o de janela: a \u00faltima\n        oferta sai de MAX(offers.id) e o topo das modas de INNER JOIN no\n        MAX(n), porque o validador do MCP rejeita `OVER (`.\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\nconst cab = $('Montar Fase 1').all()[0].json;\nconst META_IN = cab.meta;\nconst PAGE = META_IN.page;\nconst LADO = META_IN.lado;\nconst DATA_INI = META_IN.data_ini;\nconst SELECAO = META_IN.selecao;\nconst DISPONIVEL = META_IN.disponivel;\n\n/* \u2500\u2500 l\u00ea os totais da fase 1 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nconst pedidos1 = $('Montar Fase 1').all().map((i) => i.json);\nconst outs1 = $('MCP Fase 1').all();\nfunction leitura(nome) {\n  for (let i = 0; i < pedidos1.length; i++) {\n    if (pedidos1[i].queryName !== nome) continue;\n    const o = outs1[i] ? outs1[i].json : null;\n    const sc = o ? (o.structuredContent || o) : null;\n    const cols = (sc && sc.columns) || [];\n    const rows = (sc && sc.rows) || [];\n    return rows.map((r) => {\n      const obj = {};\n      for (let c = 0; c < cols.length; c++) obj[cols[c]] = r[c];\n      return obj;\n    });\n  }\n  return [];\n}\n\nconst eventos = leitura('q_eventos');\nconst vt = leitura('q_veic_total')[0] || {};\nconst lt = leitura('q_lojas_total')[0] || {};\nconst valor = leitura('q_valor')[0] || {};\n\nconst VEICULOS = vt.veiculos === undefined ? null : Number(vt.veiculos);\nconst LOJAS = lt.lojas === undefined ? null : Number(lt.lojas);\n\nif (VEICULOS === null || LOJAS === null) {\n  throw new Error('a fase 1 nao voltou completa \u2014 sem contagem nao da pra dimensionar. ' +\n    'veiculos=' + VEICULOS + ' lojas=' + LOJAS);\n}\nif (VEICULOS === 0) {\n  throw new Error('nenhum veiculo disponivel no recorte de eventos escolhido. ' +\n    'Eventos encontrados: ' + eventos.length + '. Revise EVENTOS_IDS ou HORAS_ADIANTE na fase 1.');\n}\nif (LOJAS === 0) throw new Error('nenhuma loja com oferta na janela de historico');\n\nconst PAG_VEIC = Math.ceil(VEICULOS / PAGE);\nconst PAG_LOJAS = Math.ceil(LOJAS / PAGE);\n\nconst UF_CASE =\n  \"CASE WHEN UPPER(TRIM(sa.state)) IN ('SP','MG','PR','SC','RJ','GO','RS','BA','MT','DF','CE','MS','ES','PE','PA','SE','AM','MA','RN','PB','AL','PI','RO','TO','AP','AC','RR') THEN UPPER(TRIM(sa.state))\" +\n  \" WHEN UPPER(TRIM(sa.state)) = 'S.P' THEN 'SP'\" +\n  \" WHEN UPPER(TRIM(sa.state)) = 'RIO DE JANEIRO' THEN 'RJ'\" +\n  \" WHEN UPPER(TRIM(sa.state)) = 'MATO GROSSO DO SUL' THEN 'MS'\" +\n  \" WHEN UPPER(TRIM(sa.state)) LIKE 'ESPIRITO%SANTO%' THEN 'ES'\" +\n  \" ELSE NULL END\";\n\nconst JANELA =\n  \" o.deleted_at IS NULL AND o.\" + LADO + \" IS NOT NULL\" +\n  \" AND o.created_at >= '\" + DATA_INI + \"' AND o.price > 0\";\n\nconst Q = [];\nfunction push(nome, sql, pages) {\n  for (let p = 0; p < pages; p++) {\n    Q.push({\n      queryName: nome,\n      database: 'cars2you_production',\n      sql: sql + ' LIMIT ' + PAGE + ' OFFSET ' + (p * PAGE),\n      pagina: p\n    });\n  }\n}\n\n/* \u2500\u2500 A) os ve\u00edculos do evento \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\npush('q_veiculos',\n  \"SELECT an.id AS neg_id, e.id AS evento_id, e.name AS evento,\" +\n  \" DATE_FORMAT(e.finish_date_event, '%Y-%m-%d %H:%i') AS fim_evento,\" +\n  \" a.id AS anuncio_id, a.vehicle_id AS vehicle_id,\" +\n  \" ROUND(an.value_actual, 2) AS valor,\" +\n  \" ROUND(NULLIF(an.initial_price_reference, 0), 2) AS valor_inicial,\" +\n  \" ROUND(NULLIF(a.fipe_price, 0), 2) AS fipe,\" +\n  \" v.model_id AS model_id, mo.name AS modelo,\" +\n  \" v.category_id AS category_id, cat.name AS categoria,\" +\n  \" br.name AS marca, v.model_year AS model_year, NULLIF(v.km, 0) AS km,\" +\n  \" a.shop_id AS loja_id, s.name AS loja_vendedora,\" +\n  \" COALESCE(\" + UF_CASE + \", 'N\u00e3o identificada') AS uf\" +\n  \" FROM advertisement_negotiations an\" +\n  \" INNER JOIN events e ON e.id = an.event_id AND\" + SELECAO +\n  \" INNER JOIN advertisements a ON a.id = an.advertisement_id AND a.deleted_at IS NULL\" +\n  \" INNER JOIN vehicles v ON v.id = a.vehicle_id AND v.deleted_at IS NULL\" +\n  \" LEFT JOIN models mo ON mo.id = v.model_id\" +\n  \" LEFT JOIN categories cat ON cat.id = v.category_id\" +\n  \" LEFT JOIN brands br ON br.id = v.brand_id\" +\n  \" LEFT JOIN shops s ON s.id = a.shop_id\" +\n  \" LEFT JOIN shop_addresses sa ON sa.shop_id = a.shop_id AND sa.deleted_at IS NULL\" +\n  \" WHERE\" + DISPONIVEL +\n  \" GROUP BY neg_id, evento_id, evento, fim_evento, anuncio_id, vehicle_id,\" +\n  \" valor, valor_inicial, fipe, model_id, modelo, category_id, categoria,\" +\n  \" marca, model_year, km, loja_id, loja_vendedora, uf\" +\n  \" ORDER BY an.id\", PAG_VEIC);\n\n/* \u2500\u2500 B) perfil de compra das lojas (mesmas queries do LZL3mxfbMIz4avyx) \u2500\u2500 */\npush('q_lojas',\n  \"SELECT s.id AS shop_id, MAX(s.name) AS loja,\" +\n  \" MAX(s.whitelabel_id) AS whitelabel_id, MAX(w.name) AS whitelabel,\" +\n  \" COALESCE(MAX(\" + UF_CASE + \"), 'N\u00e3o identificada') AS uf\" +\n  \" FROM shops s\" +\n  \" LEFT JOIN whitelabels w ON w.id = s.whitelabel_id\" +\n  \" LEFT JOIN shop_addresses sa ON sa.shop_id = s.id AND sa.deleted_at IS NULL\" +\n  \" WHERE s.deleted_at IS NULL\" +\n  \" AND EXISTS (SELECT 1 FROM offers o WHERE o.\" + LADO + \" = s.id\" +\n  \" AND o.deleted_at IS NULL AND o.created_at >= '\" + DATA_INI + \"' AND o.price > 0)\" +\n  \" GROUP BY s.id ORDER BY s.id\", PAG_LOJAS);\n\npush('q_ofertas',\n  \"SELECT o.\" + LADO + \" AS shop_id, COUNT(*) AS qt_ofertas\" +\n  \" FROM offers o WHERE\" + JANELA +\n  \" GROUP BY o.\" + LADO + \" ORDER BY o.\" + LADO, PAG_LOJAS);\n\nconst ULTIMAS =\n  \"(SELECT o.\" + LADO + \" AS shop_id, a.vehicle_id AS vehicle_id, MAX(o.id) AS offer_id\" +\n  \" FROM offers o\" +\n  \" INNER JOIN advertisements a ON a.id = o.advertisement_id AND a.deleted_at IS NULL\" +\n  \" WHERE\" + JANELA +\n  \" GROUP BY o.\" + LADO + \", a.vehicle_id) u\";\nconst IDADE = \"(YEAR(CURDATE()) - NULLIF(v.model_year, 0))\";\n\npush('q_perfil',\n  \"SELECT u.shop_id AS shop_id, COUNT(*) AS qt_veiculos,\" +\n  \" ROUND(AVG(ult.price), 2) AS preco_medio,\" +\n  \" ROUND(STDDEV_SAMP(ult.price), 2) AS preco_desvio,\" +\n  \" ROUND(AVG(\" + IDADE + \"), 2) AS idade_media,\" +\n  \" ROUND(STDDEV_SAMP(\" + IDADE + \"), 2) AS idade_desvio,\" +\n  \" ROUND(AVG(NULLIF(v.km, 0)), 0) AS km_medio,\" +\n  \" ROUND(STDDEV_SAMP(NULLIF(v.km, 0)), 0) AS km_desvio\" +\n  \" FROM \" + ULTIMAS +\n  \" INNER JOIN offers ult ON ult.id = u.offer_id\" +\n  \" INNER JOIN vehicles v ON v.id = u.vehicle_id AND v.deleted_at IS NULL\" +\n  \" GROUP BY u.shop_id ORDER BY u.shop_id\", PAG_LOJAS);\n\nfunction moda(nome, campo, tabela) {\n  const AG =\n    \"(SELECT o.\" + LADO + \" AS shop_id, v.\" + campo + \" AS item_id, COUNT(*) AS n\" +\n    \" FROM offers o\" +\n    \" INNER JOIN advertisements a ON a.id = o.advertisement_id AND a.deleted_at IS NULL\" +\n    \" INNER JOIN vehicles v ON v.id = a.vehicle_id AND v.deleted_at IS NULL\" +\n    \" WHERE\" + JANELA + \" AND v.\" + campo + \" IS NOT NULL\" +\n    \" GROUP BY o.\" + LADO + \", v.\" + campo + \")\";\n  push(nome,\n    \"SELECT ag.shop_id AS shop_id, ag.item_id AS item_id, cat.name AS nome, ag.n AS n\" +\n    \" FROM \" + AG + \" ag\" +\n    \" INNER JOIN (SELECT t.shop_id AS shop_id, MAX(t.n) AS mx FROM \" + AG + \" t\" +\n    \" GROUP BY t.shop_id) top ON top.shop_id = ag.shop_id AND ag.n = top.mx\" +\n    \" LEFT JOIN \" + tabela + \" cat ON cat.id = ag.item_id\" +\n    \" ORDER BY ag.shop_id, ag.item_id\", PAG_LOJAS);\n}\nmoda('q_modelo', 'model_id', 'models');\nmoda('q_categoria', 'category_id', 'categories');\n\n/* \u2500\u2500 guardas \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nconst comJanela = Q.filter((q) => /OVER\\s*\\(/i.test(q.sql));\nif (comJanela.length) {\n  throw new Error('funcao de janela detectada (o MCP rejeita): ' + comJanela[0].queryName);\n}\nif (PAGE > 50) throw new Error('PAGE > 50: o MCP corta a resposta em 50 linhas');\n\nconst META = {\n  selecao: SELECAO,\n  eventos: eventos,\n  meses_historico: META_IN.meses_historico,\n  data_ini: DATA_INI,\n  page: PAGE,\n  lado: LADO,\n  esperado_veiculos: VEICULOS,\n  esperado_negociacoes: vt.negociacoes === undefined ? null : Number(vt.negociacoes),\n  esperado_lojas: LOJAS,\n  esperado_ofertas: lt.ofertas === undefined ? null : Number(lt.ofertas),\n  pag_veic: PAG_VEIC,\n  pag_lojas: PAG_LOJAS,\n  cobertura_valor: valor,\n  chamadas: Q.length + 4\n};\n\nreturn Q.map((q, i) => ({\n  json: { queryName: q.queryName, database: q.database, sql: q.sql, pagina: q.pagina, idx: i, total: Q.length, meta: META }\n}));\n"
    }
  }
});

const mcp2 = node({
  type: '@n8n/n8n-nodes-langchain.mcpClient',
  version: 1.1,
  config: {
    name: 'MCP Fase 2',
    position: [896, 0],
    onError: 'continueRegularOutput',
    parameters: {
      serverTransport: 'sse',
      endpointUrl: 'https://mcp-cars2you-readonly.cars2you.com.br/sse',
      authentication: 'bearerAuth',
      tool: { __rl: true, mode: 'id', value: 'run_query', cachedResultName: 'run_query' },
      inputMode: 'json',
      jsonInput: expr('{{ JSON.stringify({ sql: $json.sql, database: $json.database }) }}'),
      options: { timeout: 60000 }
    },
    credentials: { httpBearerAuth: newCredential('MCP Cars2You Readonly - BD SQL', 'Cc8CxzVDwvA3EysZ') }
  }
});

const html = node({
  type: 'n8n-nodes-base.code',
  version: 2,
  config: {
    name: 'Montar HTML',
    position: [1120, 0],
    parameters: {
      mode: 'runOnceForAllItems',
      language: 'javaScript',
      jsCode: "/* \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n   N\u00d3 \"Montar HTML\" \u2014 ader\u00eancia loja x ve\u00edculo\n\n   \u2500\u2500\u2500 A F\u00d3RMULA \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n   Para cada par (ve\u00edculo, loja), cinco componentes. Cada um tem uma\n   ADER\u00caNCIA (0 a 1, o quanto o ve\u00edculo parece com o que a loja compra) e\n   um PESO (0 a 1, o quanto aquele indicador \u00e9 confi\u00e1vel NAQUELA loja).\n\n   Pre\u00e7o, idade e km:\n     ader\u00eancia = 1 / (1 + |valor \u2212 m\u00e9dia| / desvio)     \u2190 dist\u00e2ncia em desvios\n     peso      = 1 / (1 + desvio / m\u00e9dia)               \u2190 CV: dispers\u00e3o relativa\n\n     A leitura do peso \u00e9 a chave. Loja com pre\u00e7o apertado (desvio pequeno\n     perto da m\u00e9dia) \u00e9 previs\u00edvel, ent\u00e3o acertar o pre\u00e7o dela vale muito.\n     Loja que compra de R$ 20 mil a R$ 400 mil tem CV alto: o pre\u00e7o n\u00e3o\n     informa quase nada e o peso cai sozinho.\n\n   Modelo e categoria:\n     ader\u00eancia = 1 se o ve\u00edculo \u00e9 do item que a loja mais oferta, sen\u00e3o 0\n     peso      = o % de ofertas da loja naquele item\n\n     Quem concentra 60% das ofertas num modelo d\u00e1 sinal forte. Quem\n     espalha em 40 modelos d\u00e1 sinal fraco, e o peso reflete isso.\n\n   score = \u03a3(peso \u00d7 ader\u00eancia) / \u03a3(peso), normalizado 0\u2013100.\n\n   \u2500\u2500\u2500 UMA ADI\u00c7\u00c3O MINHA, QUE N\u00c3O ESTAVA NO PEDIDO \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n   `CONFIANCA_MIN`: loja com pouqu\u00edssimo hist\u00f3rico pode acertar por sorte\n   e liderar o ranking sem significar nada. Ent\u00e3o o score \u00e9 multiplicado\n   por min(1, qt_veiculos / CONFIANCA_MIN). Com CONFIANCA_MIN = 5, uma\n   loja de 1 ve\u00edculo entra com 20% do score. Para desligar, ponha 1.\n\n   Efeito colateral intencional: loja generalista pontua baixo em tudo,\n   porque de fato n\u00e3o d\u00e1 para prever o que ela compra. Volume de ofertas\n   aparece como coluna, mas N\u00c3O entra no score \u2014 sen\u00e3o o ranking vira\n   \"quem compra muito\" em vez de \"quem compra esse\".\n   \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550 */\n\nconst TOP_N = 10;\nconst CONFIANCA_MIN = 5;\n\nconst pedidos = $('Montar Fase 2').all().map((i) => i.json);\nconst outs = $('MCP Fase 2').all();\nconst META = pedidos.length ? pedidos[0].meta : {};\n\nfunction erroTexto(e) {\n  if (!e) return null;\n  if (typeof e === 'string') return e.slice(0, 300);\n  if (Array.isArray(e)) return e.map(erroTexto).join(' | ').slice(0, 300);\n  if (e.message) return String(e.message).slice(0, 300);\n  if (e.error) return erroTexto(e.error);\n  try { return JSON.stringify(e).slice(0, 300); } catch (x) { return String(e).slice(0, 300); }\n}\n\n/* \u2500\u2500 ingest\u00e3o \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nconst dados = {};\nconst diag = {};\nfor (let i = 0; i < pedidos.length; i++) {\n  const nome = pedidos[i].queryName;\n  if (!dados[nome]) dados[nome] = [];\n  if (!diag[nome]) diag[nome] = { queryName: nome, chamadas: 0, linhas: 0, vazias: 0, truncadas: 0, erro: null };\n  diag[nome].chamadas++;\n  const o = outs[i] ? outs[i].json : null;\n  const sc = o ? (o.structuredContent || o) : null;\n  const err = o ? (o.error || (sc && sc.error)) : null;\n  if (err && !diag[nome].erro) diag[nome].erro = erroTexto(err);\n  if (sc && sc.truncated) diag[nome].truncadas++;\n  const cols = (sc && sc.columns) || [];\n  const rows = (sc && sc.rows) || [];\n  if (!rows.length) { diag[nome].vazias++; continue; }\n  diag[nome].linhas += rows.length;\n  for (let r = 0; r < rows.length; r++) {\n    const obj = {};\n    for (let c = 0; c < cols.length; c++) obj[cols[c]] = rows[r][c];\n    dados[nome].push(obj);\n  }\n}\nconst diagnostico = Object.keys(diag).map((k) => {\n  const d = diag[k];\n  d.veredito = d.erro ? 'ERRO'\n    : (d.truncadas ? 'RESPOSTA CORTADA - pagina maior que o teto do MCP'\n      : (d.linhas === 0 ? 'ZERO LINHAS' : 'ok'));\n  return d;\n});\n\nconst num = (v) => (v === null || v === undefined || v === '' ? null : Number(v));\nfunction indexa(arr, chave) {\n  const m = {};\n  for (let i = 0; i < arr.length; i++) m[String(arr[i][chave])] = arr[i];\n  return m;\n}\nfunction primeiraPorLoja(arr) {\n  const m = {};\n  for (let i = 0; i < arr.length; i++) {\n    const k = String(arr[i].shop_id);\n    if (!m[k]) m[k] = arr[i];\n  }\n  return m;\n}\n\n/* \u2500\u2500 perfil das lojas \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nconst iOf = indexa(dados.q_ofertas || [], 'shop_id');\nconst iPe = indexa(dados.q_perfil || [], 'shop_id');\nconst iMo = primeiraPorLoja(dados.q_modelo || []);\nconst iCa = primeiraPorLoja(dados.q_categoria || []);\n\nfunction pesoNum(media, desvio) {\n  if (!media || media <= 0) return 0;\n  if (desvio === null || desvio === undefined || desvio < 0) return 0;\n  return 1 / (1 + (desvio / media));\n}\n\nconst lojas = (dados.q_lojas || []).map((s) => {\n  const k = String(s.shop_id);\n  const of = iOf[k] || {};\n  const pe = iPe[k] || {};\n  const mo = iMo[k] || {};\n  const ca = iCa[k] || {};\n  const qtOfertas = num(of.qt_ofertas);\n  const pct = (n) => (qtOfertas && n ? Number(n) / qtOfertas : 0);\n  const precoMedio = num(pe.preco_medio);\n  const precoDesvio = num(pe.preco_desvio);\n  const idadeMedia = num(pe.idade_media);\n  const idadeDesvio = num(pe.idade_desvio);\n  const kmMedio = num(pe.km_medio);\n  const kmDesvio = num(pe.km_desvio);\n  const qtVeiculos = num(pe.qt_veiculos);\n  return {\n    loja_id: num(s.shop_id),\n    loja: s.loja,\n    whitelabel: s.whitelabel || ('whitelabel #' + s.whitelabel_id),\n    uf: s.uf || 'Nao identificada',\n    qt_ofertas: qtOfertas,\n    qt_veiculos: qtVeiculos,\n    preco_medio: precoMedio, preco_desvio: precoDesvio,\n    idade_media: idadeMedia, idade_desvio: idadeDesvio,\n    km_medio: kmMedio, km_desvio: kmDesvio,\n    modelo_id: num(mo.item_id), modelo: mo.nome || null, pct_modelo: pct(mo.n),\n    categoria_id: num(ca.item_id), categoria: ca.nome || null, pct_categoria: pct(ca.n),\n    p_preco: pesoNum(precoMedio, precoDesvio),\n    p_idade: pesoNum(idadeMedia, idadeDesvio),\n    p_km: pesoNum(kmMedio, kmDesvio),\n    confianca: Math.min(1, (qtVeiculos || 0) / CONFIANCA_MIN)\n  };\n}).filter((l) => l.qt_veiculos);\n\n/* \u2500\u2500 ve\u00edculos: dedupe por negocia\u00e7\u00e3o \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nconst vistos = {};\nlet dupVeic = 0;\nconst ANO = new Date().getFullYear();\nconst veiculos = [];\n(dados.q_veiculos || []).forEach((r) => {\n  const k = String(r.neg_id);\n  if (vistos[k]) { dupVeic++; return; }\n  vistos[k] = 1;\n  const my = num(r.model_year);\n  veiculos.push({\n    neg_id: num(r.neg_id), vehicle_id: num(r.vehicle_id), anuncio_id: num(r.anuncio_id),\n    evento_id: num(r.evento_id), evento: r.evento, fim_evento: r.fim_evento,\n    valor: num(r.valor), valor_inicial: num(r.valor_inicial), fipe: num(r.fipe),\n    model_id: num(r.model_id), modelo: r.modelo,\n    category_id: num(r.category_id), categoria: r.categoria,\n    marca: r.marca, model_year: my, idade: my ? ANO - my : null, km: num(r.km),\n    loja_id: num(r.loja_id), loja_vendedora: r.loja_vendedora, uf: r.uf\n  });\n});\n\n/* \u2500\u2500 o c\u00e1lculo \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nfunction adNum(valor, media, desvio) {\n  if (valor === null || media === null || !media) return null;\n  if (desvio && desvio > 0) return 1 / (1 + (Math.abs(valor - media) / desvio));\n  return 1 / (1 + (Math.abs(valor - media) / media));\n}\n\nfunction pontua(v, l) {\n  const comps = [];\n  const aP = adNum(v.valor, l.preco_medio, l.preco_desvio);\n  if (aP !== null && l.p_preco > 0) comps.push({ k: 'preco', a: aP, p: l.p_preco });\n  const aI = adNum(v.idade, l.idade_media, l.idade_desvio);\n  if (aI !== null && l.p_idade > 0) comps.push({ k: 'idade', a: aI, p: l.p_idade });\n  const aK = adNum(v.km, l.km_medio, l.km_desvio);\n  if (aK !== null && l.p_km > 0) comps.push({ k: 'km', a: aK, p: l.p_km });\n  if (l.pct_modelo > 0 && v.model_id !== null) {\n    comps.push({ k: 'modelo', a: (v.model_id === l.modelo_id ? 1 : 0), p: l.pct_modelo });\n  }\n  if (l.pct_categoria > 0 && v.category_id !== null) {\n    comps.push({ k: 'categoria', a: (v.category_id === l.categoria_id ? 1 : 0), p: l.pct_categoria });\n  }\n  if (!comps.length) return null;\n  let somaP = 0, somaPA = 0;\n  for (let i = 0; i < comps.length; i++) { somaP += comps[i].p; somaPA += comps[i].p * comps[i].a; }\n  if (somaP <= 0) return null;\n  const bruto = somaPA / somaP;\n  const det = {};\n  for (let i = 0; i < comps.length; i++) det[comps[i].k] = Math.round(comps[i].a * 100);\n  return {\n    score: Math.round(bruto * l.confianca * 1000) / 10,\n    score_bruto: Math.round(bruto * 1000) / 10,\n    det: det\n  };\n}\n\nveiculos.forEach((v) => {\n  const cand = [];\n  for (let i = 0; i < lojas.length; i++) {\n    const r = pontua(v, lojas[i]);\n    if (!r || r.score <= 0) continue;\n    cand.push({\n      loja_id: lojas[i].loja_id, loja: lojas[i].loja, uf: lojas[i].uf,\n      whitelabel: lojas[i].whitelabel, qt_ofertas: lojas[i].qt_ofertas,\n      qt_veiculos: lojas[i].qt_veiculos,\n      preco_medio: lojas[i].preco_medio, idade_media: lojas[i].idade_media,\n      km_medio: lojas[i].km_medio, modelo: lojas[i].modelo, categoria: lojas[i].categoria,\n      score: r.score, score_bruto: r.score_bruto, det: r.det,\n      amostra_baixa: lojas[i].qt_veiculos < CONFIANCA_MIN\n    });\n  }\n  cand.sort((a, b) => b.score - a.score);\n  v.top = cand.slice(0, TOP_N);\n  v.candidatos = cand.length;\n  v.melhor = v.top.length ? v.top[0].score : null;\n});\n\nveiculos.sort((a, b) => (b.melhor || 0) - (a.melhor || 0));\n\n/* \u2500\u2500 completude \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\nconst falhas = [];\nif (META.esperado_veiculos && veiculos.length !== META.esperado_veiculos) {\n  falhas.push('coletei ' + veiculos.length + ' veiculos mas a fase 1 contou ' +\n    META.esperado_veiculos + ': faltam ' + (META.esperado_veiculos - veiculos.length));\n}\nif (META.esperado_lojas && lojas.length !== META.esperado_lojas) {\n  falhas.push('perfil montado para ' + lojas.length + ' lojas mas a fase 1 contou ' +\n    META.esperado_lojas);\n}\nconst semValor = veiculos.filter((v) => !v.valor).length;\nif (semValor) falhas.push(semValor + ' veiculo(s) sem value_actual: ficam sem o componente de preco');\nif (dupVeic) falhas.push(dupVeic + ' linha(s) duplicada(s) de veiculo descartada(s)');\n\nconst DADOS = {\n  gerado_em: new Date().toISOString(),\n  meta: META,\n  parametros: { top_n: TOP_N, confianca_min: CONFIANCA_MIN },\n  resumo: {\n    veiculos: veiculos.length,\n    veiculos_esperados: META.esperado_veiculos || null,\n    lojas_no_ranking: lojas.length,\n    lojas_esperadas: META.esperado_lojas || null,\n    eventos: (META.eventos || []).length,\n    sem_valor: semValor,\n    score_medio: veiculos.length\n      ? Math.round((veiculos.reduce((s, v) => s + (v.melhor || 0), 0) / veiculos.length) * 10) / 10\n      : null\n  },\n  falhas: falhas,\n  diagnostico: diagnostico,\n  eventos: META.eventos || [],\n  veiculos: veiculos\n};\n\nconst problemas = diagnostico.filter((d) => d.veredito !== 'ok').length + falhas.length;\nconst DADOS_JSON = JSON.stringify(DADOS).split('</').join('<\\\\/');\n\nconst CSS = [\n  ':root{--bg:#0f1216;--card:#171c22;--line:#252d36;--tx:#e6edf3;--dim:#8b98a5;',\n  '--ac:#4da3ff;--err:#ff6b6b;--ok:#3fb950;--warn:#ffb454}',\n  '*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--tx);',\n  'font:14px/1.5 -apple-system,Segoe UI,Roboto,sans-serif;padding:24px}',\n  'h1{font-size:20px;margin:0 0 4px}h2{font-size:15px;margin:26px 0 10px;color:var(--ac)}',\n  '.sub{color:var(--dim);font-size:12px;margin-bottom:18px}',\n  '.kpis{display:flex;flex-wrap:wrap;gap:10px;margin-bottom:8px}',\n  '.kpi{background:var(--card);border:1px solid var(--line);border-radius:8px;padding:12px 16px;min-width:120px}',\n  '.kpi b{display:block;font-size:22px;font-weight:600}',\n  '.kpi span{color:var(--dim);font-size:11px;text-transform:uppercase;letter-spacing:.4px}',\n  'table{border-collapse:collapse;width:100%;font-size:12.5px}',\n  'th,td{padding:6px 9px;border-bottom:1px solid var(--line);text-align:right;white-space:nowrap}',\n  'th{background:var(--card);position:sticky;top:0;font-weight:600;cursor:pointer}',\n  'th.tx,td.tx{text-align:left}',\n  'tbody tr{cursor:pointer}tbody tr:hover{background:#1c232b}',\n  'tr.sel{background:#1e2836}',\n  '.wrap{overflow:auto;max-height:60vh;border:1px solid var(--line);border-radius:8px}',\n  '.box{background:var(--card);border:1px solid var(--line);border-radius:8px;padding:14px 16px;margin-bottom:14px}',\n  '.box li{margin-bottom:5px}.err{border-color:var(--err)}.err h2{color:var(--err);margin-top:0}',\n  '.tag{display:inline-block;padding:1px 6px;border-radius:99px;font-size:10.5px;background:#243040;color:var(--ac)}',\n  '.tag.w{background:#3a2f18;color:var(--warn)}',\n  'select,input{background:#0d1117;color:var(--tx);border:1px solid var(--line);',\n  'border-radius:6px;padding:6px 9px;font-size:13px;margin-right:8px}',\n  '.dim{color:var(--dim)}.mono{font-family:ui-monospace,Consolas,monospace;font-size:11.5px}',\n  'code{font-family:ui-monospace,Consolas,monospace;font-size:12px;background:#0d1117;padding:1px 5px;border-radius:4px}',\n  '.bar{display:inline-block;height:8px;background:var(--ac);border-radius:2px;vertical-align:middle}',\n  '.grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}',\n  '@media(max-width:1100px){.grid{grid-template-columns:1fr}}'\n].join('');\n\nconst APP = [\n  'const $=(s)=>document.querySelector(s);',\n  'const nf=(v,d)=>v===null||v===undefined?\"\\\\u2014\":Number(v).toLocaleString(\"pt-BR\",{minimumFractionDigits:d||0,maximumFractionDigits:d||0});',\n  'const money=(v)=>v===null||v===undefined?\"\\\\u2014\":\"R$ \"+nf(v,0);',\n  'const pc=(v)=>v===null||v===undefined?\"\\\\u2014\":nf(v,1)+\"%\";',\n  '$(\"#ger\").textContent=new Date(D.gerado_em).toLocaleString(\"pt-BR\");',\n  'const R=D.resumo;',\n  '$(\"#kpis\").innerHTML=[[\"veiculos\",nf(R.veiculos)],[\"eventos\",nf(R.eventos)],[\"lojas no ranking\",nf(R.lojas_no_ranking)],[\"score medio do 1o lugar\",R.score_medio===null?\"\\\\u2014\":nf(R.score_medio,1)]].map(k=>\"<div class=\\'kpi\\'><b>\"+k[1]+\"</b><span>\"+k[0]+\"</span></div>\").join(\"\");',\n  'const av=[];D.falhas.forEach(f=>av.push(\"<li>\"+f+\"</li>\"));',\n  'D.diagnostico.filter(d=>d.veredito!==\"ok\").forEach(p=>av.push(\"<li><code>\"+p.queryName+\"</code>: \"+p.veredito+(p.erro?\" \\\\u2014 <span class=\\'mono\\'>\"+p.erro+\"</span>\":\"\")+\"</li>\"));',\n  'if(av.length){$(\"#alerta\").innerHTML=\"<div class=\\'box err\\'><h2>\"+av.length+\" problema(s) \\\\u2014 leia antes de usar</h2><ul>\"+av.join(\"\")+\"</ul></div>\";}',\n  '$(\"#eventos\").innerHTML=D.eventos.map(e=>\"<li><b>\"+e.evento+\"</b> <span class=\\'dim mono\\'>#\"+e.evento_id+\"</span> \\\\u2014 fecha \"+e.fim_evento+\"</li>\").join(\"\");',\n  'function tabela(el,cols,rows,onrow){el.innerHTML=\"<thead><tr>\"+cols.map(c=>\"<th\"+(c.tx?\" class=\\'tx\\'\":\"\")+\">\"+c.t+\"</th>\").join(\"\")+\"</tr></thead><tbody>\"+rows.map((r,i)=>\"<tr data-i=\\'\"+i+\"\\'>\"+cols.map(c=>\"<td\"+(c.tx?\" class=\\'tx\\'\":\"\")+\">\"+c.f(r)+\"</td>\").join(\"\")+\"</tr>\").join(\"\")+\"</tbody>\";if(onrow){Array.prototype.forEach.call(el.querySelectorAll(\"tbody tr\"),function(tr){tr.onclick=function(){Array.prototype.forEach.call(el.querySelectorAll(\"tbody tr\"),function(x){x.className=\"\";});tr.className=\"sel\";onrow(rows[Number(tr.getAttribute(\"data-i\"))]);};});}}',\n  'const VCOLS=[',\n  '{t:\"Veiculo\",tx:1,k:\"modelo\",f:r=>(r.marca?r.marca+\" \":\"\")+(r.modelo||\"\\\\u2014\")+\" <span class=\\'dim mono\\'>#\"+r.vehicle_id+\"</span>\"},',\n  '{t:\"Categoria\",tx:1,k:\"categoria\",f:r=>r.categoria||\"\\\\u2014\"},',\n  '{t:\"Ano\",k:\"model_year\",f:r=>r.model_year||\"\\\\u2014\"},',\n  '{t:\"Idade\",k:\"idade\",f:r=>nf(r.idade)},',\n  '{t:\"Km\",k:\"km\",f:r=>nf(r.km)},',\n  '{t:\"Valor\",k:\"valor\",f:r=>money(r.valor)},',\n  '{t:\"UF\",tx:1,k:\"uf\",f:r=>r.uf},',\n  '{t:\"Evento\",tx:1,k:\"evento\",f:r=>r.evento},',\n  '{t:\"Melhor score\",k:\"melhor\",f:r=>r.melhor===null?\"\\\\u2014\":\"<span class=\\'bar\\' style=\\'width:\"+Math.round(r.melhor/2)+\"px\\'></span> \"+nf(r.melhor,1)},',\n  '{t:\"Lojas\",k:\"candidatos\",f:r=>nf(r.candidatos)}];',\n  'const uniq=(k)=>Array.from(new Set(D.veiculos.map(r=>r[k]))).sort();',\n  '$(\"#f_ev\").innerHTML=\"<option value=\\'\\'>todos os eventos</option>\"+uniq(\"evento\").map(v=>\"<option>\"+v+\"</option>\").join(\"\");',\n  '$(\"#f_cat\").innerHTML=\"<option value=\\'\\'>todas as categorias</option>\"+uniq(\"categoria\").filter(v=>v).map(v=>\"<option>\"+v+\"</option>\").join(\"\");',\n  'var ord=\"melhor\",asc=false,atual=null;',\n  'function detalhe(v){atual=v;',\n  'if(!v){$(\"#det\").innerHTML=\"<div class=\\'dim\\'>Clique num veiculo para ver as lojas mais aderentes.</div>\";return;}',\n  '$(\"#det_tit\").innerHTML=(v.marca?v.marca+\" \":\"\")+(v.modelo||\"?\")+\" \"+(v.model_year||\"\")+\" \\\\u2014 \"+money(v.valor)+\" \\\\u2014 \"+nf(v.km)+\" km <span class=\\'dim\\'>\"+v.evento+\"</span>\";',\n  'if(!v.top.length){$(\"#det\").innerHTML=\"<div class=\\'box\\'>Nenhuma loja pontuou para este veiculo.</div>\";return;}',\n  'const cols=[{t:\"#\",f:(r)=>r.pos},{t:\"Loja\",tx:1,f:r=>r.loja+\" <span class=\\'dim mono\\'>#\"+r.loja_id+\"</span>\"+(r.amostra_baixa?\" <span class=\\'tag w\\'>amostra \"+r.qt_veiculos+\"</span>\":\"\")},{t:\"UF\",tx:1,f:r=>r.uf},{t:\"Score\",f:r=>\"<span class=\\'bar\\' style=\\'width:\"+Math.round(r.score/2)+\"px\\'></span> \"+nf(r.score,1)},{t:\"Bruto\",f:r=>nf(r.score_bruto,1)},{t:\"Preco\",f:r=>r.det.preco===undefined?\"\\\\u2014\":nf(r.det.preco)},{t:\"Idade\",f:r=>r.det.idade===undefined?\"\\\\u2014\":nf(r.det.idade)},{t:\"Km\",f:r=>r.det.km===undefined?\"\\\\u2014\":nf(r.det.km)},{t:\"Modelo\",f:r=>r.det.modelo===undefined?\"\\\\u2014\":nf(r.det.modelo)},{t:\"Categ.\",f:r=>r.det.categoria===undefined?\"\\\\u2014\":nf(r.det.categoria)},{t:\"Perfil da loja\",tx:1,f:r=>money(r.preco_medio)+\" \\\\u00b7 \"+nf(r.idade_media,1)+\" anos \\\\u00b7 \"+nf(r.km_medio)+\" km \\\\u00b7 \"+(r.modelo||\"?\")},{t:\"Ofertas 6m\",f:r=>nf(r.qt_ofertas)}];',\n  'const rows=v.top.map((r,i)=>{const o={};for(const k in r)o[k]=r[k];o.pos=i+1;return o;});',\n  'tabela($(\"#t_top\"),cols,rows);',\n  '$(\"#det\").innerHTML=\"\";$(\"#det_wrap\").style.display=\"\";}',\n  'function render(){',\n  'const ev=$(\"#f_ev\").value,cat=$(\"#f_cat\").value,q=$(\"#f_q\").value.toLowerCase();',\n  'const rows=D.veiculos.filter(r=>(!ev||r.evento===ev)&&(!cat||r.categoria===cat)&&(!q||((r.marca||\"\")+\" \"+(r.modelo||\"\")).toLowerCase().indexOf(q)>=0)).slice()',\n  '.sort((a,b)=>{const x=a[ord],y=b[ord];const s=(x===null||x===undefined)?1:((y===null||y===undefined)?-1:(typeof x===\"string\"?String(x).localeCompare(String(y)):x-y));return asc?s:-s;});',\n  'tabela($(\"#t_v\"),VCOLS,rows,detalhe);',\n  '$(\"#cont\").textContent=rows.length+\" de \"+D.veiculos.length+\" veiculos\";',\n  'Array.prototype.forEach.call($(\"#t_v\").querySelectorAll(\"th\"),function(th,i){th.onclick=function(){const k=VCOLS[i].k;asc=(k===ord)?!asc:false;ord=k;render();};});',\n  'if(rows.length)detalhe(rows[0]);}',\n  '$(\"#f_ev\").onchange=render;$(\"#f_cat\").onchange=render;$(\"#f_q\").oninput=render;render();'\n].join('\\n');\n\nconst html = [\n  '<!doctype html><html lang=\"pt-BR\"><head><meta charset=\"utf-8\">',\n  '<meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">',\n  '<title>Veiculos em evento - lojas mais aderentes</title>',\n  '<style>' + CSS + '</style></head><body>',\n  '<h1>Veiculos em evento &mdash; lojas com maior chance de comprar</h1>',\n  '<div class=\"sub\">Perfil de compra dos ultimos ' + (META.meses_historico || 6) + ' meses (desde ' + (META.data_ini || '?') + ') &middot; preco do veiculo = <code>value_actual</code> &middot; gerado em <span id=\"ger\"></span></div>',\n  '<div id=\"alerta\"></div>',\n  '<div class=\"kpis\" id=\"kpis\"></div>',\n  '<h2>Eventos no recorte</h2><div class=\"box\"><ul id=\"eventos\"></ul></div>',\n  '<h2>Como o score e calculado</h2>',\n  '<div class=\"box\"><ul>',\n  '<li>Cinco componentes: <b>preco, idade, km, modelo e categoria</b>. Cada um tem uma <b>aderencia</b> (0 a 1, o quanto o veiculo parece com o que a loja compra) e um <b>peso</b> (0 a 1, o quanto aquele indicador e confiavel naquela loja).</li>',\n  '<li><b>Numericos:</b> aderencia = <code>1 / (1 + |valor &minus; media| / desvio)</code>. Peso = <code>1 / (1 + desvio/media)</code>. Loja com preco apertado e previsivel, entao acertar o preco dela vale muito; loja que compra de tudo tem peso baixo porque o indicador nao informa.</li>',\n  '<li><b>Modelo e categoria:</b> aderencia = 1 se o veiculo e do item que a loja mais oferta, senao 0. Peso = o <b>% de ofertas</b> da loja naquele item.</li>',\n  '<li><b>Score</b> = &Sigma;(peso &times; aderencia) / &Sigma;(peso), de 0 a 100. As colunas <code>Preco</code>, <code>Idade</code>, <code>Km</code>, <code>Modelo</code> e <code>Categ.</code> no detalhe mostram a aderencia de cada componente, para voce ver <i>por que</i> a loja apareceu.</li>',\n  '<li><b>Confianca:</b> o score e multiplicado por <code>min(1, veiculos / ' + CONFIANCA_MIN + ')</code>, para uma loja de historico minusculo nao liderar por sorte. A coluna <code>Bruto</code> mostra o score antes disso, e o selo <span class=\"tag w\">amostra N</span> marca quem foi penalizado. <b>Isso foi adicao minha, nao estava no pedido.</b></li>',\n  '<li><b>Volume nao entra no score.</b> <code>Ofertas 6m</code> esta na tela como desempate visual, mas somar volume transformaria o ranking em \"quem compra muito\" em vez de \"quem compra esse\".</li>',\n  '</ul></div>',\n  '<h2>Veiculos</h2>',\n  '<div style=\"margin-bottom:10px\"><select id=\"f_ev\"></select><select id=\"f_cat\"></select>',\n  '<input id=\"f_q\" placeholder=\"filtrar por marca ou modelo\" size=\"26\">',\n  '<span class=\"dim\" id=\"cont\"></span></div>',\n  '<div class=\"wrap\"><table id=\"t_v\"></table></div>',\n  '<h2>Lojas mais aderentes &mdash; <span id=\"det_tit\" class=\"dim\"></span></h2>',\n  '<div id=\"det\"></div>',\n  '<div id=\"det_wrap\" class=\"wrap\" style=\"max-height:none\"><table id=\"t_top\"></table></div>',\n  '<h2>Diagnostico das queries</h2>',\n  '<div class=\"wrap\" style=\"max-height:none\"><table id=\"t_diag\"></table></div>',\n  '<script>const D=' + DADOS_JSON + ';</' + 'script>',\n  '<script>' + APP + '\\ntabela($(\"#t_diag\"),[{t:\"Query\",tx:1,f:r=>r.queryName},{t:\"Chamadas\",f:r=>nf(r.chamadas)},{t:\"Linhas\",f:r=>nf(r.linhas)},{t:\"Vazias\",f:r=>nf(r.vazias)},{t:\"Cortadas\",f:r=>nf(r.truncadas)},{t:\"Veredito\",tx:1,f:r=>r.veredito===\"ok\"?\"<span style=\\'color:var(--ok)\\'>ok</span>\":\"<span style=\\'color:var(--err)\\'>\"+r.veredito+\"</span>\"}],D.diagnostico);</' + 'script>',\n  '</body></html>'\n].join('\\n');\n\nreturn [{\n  json: {\n    resumo: DADOS.resumo,\n    parametros: DADOS.parametros,\n    falhas: falhas,\n    diagnostico: diagnostico,\n    problemas: problemas,\n    html_bytes: html.length,\n    html: html,\n    DADOS: DADOS\n  }\n}];\n"
    }
  }
});

export default workflow('rel-veiculos-aderencia', 'RELATORIO Veiculos em evento - lojas mais aderentes')
  .add(rodar)
  .to(fase1)
  .to(mcp1)
  .to(fase2)
  .to(mcp2)
  .to(html);
