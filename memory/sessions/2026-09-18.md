# Sessão — 2026-09-18

> Continuação direta da madrugada de 17/09 — a sessão atravessou a meia-noite.
> O que é do dia 17 está em [`2026-09-17.md`](2026-09-17.md).

## 🌙 Madrugada — conexão à Cars2You, painel unificado, e duas correções minhas

### Resumo
O Thomas abriu a conexão direta ao banco da **Cars2You**, o que destravou tudo
que estava pela metade: a extração com `descricao` e `laudo`, a comparação real
entre os dois esquemas, e o fechamento de uma ressalva de 09/09. Saiu um
**painel único** com as duas análises e as duas bases. E duas afirmações
minhas caíram no caminho.

### Decisões tomadas
- **O módulo de conexão virou multi-base**, em `automations/bancos/conexao.py`.
  Duplicar o arquivo criaria duas versões livres para divergir. Cada base tem
  seu próprio prefixo de variável (`DLC_`, `C2Y_`) para um arquivo de
  credencial não poder ser usado no lugar do outro por engano.
- **`roda-estudo.py` virou multi-base** (`--base cars2you|dealers`). A amostra
  é SEMPRE re-derivada na base escolhida.
- **O controle do painel é por (base, modelo)**, não só por modelo. Um Gol da
  Cars2You e um da Dealers são operações diferentes; num grupo só, a diferença
  entre as BASES vazaria para dentro do efeito de km e idade.
- **"Não consta na base" em vez de 0** quando um sinal não aparece numa base.
  Zero diria "medi e deu zero" — afirmação diferente, e mais forte.
- **A ordem dos grupos é fixa**, a que o Thomas definiu. Ordenar por efeito
  reorganizava a lista a cada troca de base.
- **Bootstrap embutido** (CSS + JS), vendorizado em
  `automations/n8n-sdk/precificacao/vendor/`. Só o CSS não bastava: quem troca
  de aba e abre o glossário é o JS.
- **`Crivo` e `Baixa / sinistrado` saíram** dos sinais, a pedido. Ficaram
  comentados no dicionário, com os números, e não apagados.

### 🔴 Duas afirmações minhas que o dado derrubou

**1. "A Dealers é um superconjunto com versão mais nova."** Errado. Com as duas
conexões deu para comparar ao vivo:

| | |
|---|---:|
| Cars2You | 193 tabelas · 2.075 colunas |
| Dealers | 193 tabelas · 2.074 colunas |
| Em comum | 192 (só `base_cars2you` × `base_dealersclub` diferem) |
| Com colunas diferentes | **1** — `notifications`, 2 a mais na Cars2You |

As 44 tabelas que atribuí à Dealers existem na Cars2You também. **O incompleto
é o nosso `schema.md`: 149 de 193.** Corrigido em 5 arquivos.

**2. "A `descricao` da Cars2You não pode ser usada."** Ela ficou fora da sonda
10 de propósito ("texto livre; não é categoria nem número"), mas o texto foi
colhido por OUTRA sonda, agregado em 1.725 células. Eu olhei a extração linha a
linha, vi a coluna faltando, e concluí "não dá" — estava a um arquivo de
distância.

### O que foi entregue
- **`painel-precificacao.html`** — duas telas num arquivo, seletor de base
  único, glossário por tela, Bootstrap embutido, 9.367 vendas (Cars2You 4.596 +
  Dealers 4.771), deságio 30,4%.
- **`colunas-ocultas.md`** — fecha a ressalva 2 de 09/09. São **79** colunas em
  4 tabelas, não 49: a estimativa vinha do rótulo "N more..." do diagrama.
- **`tabelas-nao-documentadas.md`** — as 44 que faltam no `schema.md`, com
  contagem real de linhas. 19 estão vazias.
- **`email-precificacao.md`** — rascunho para o Thomas revisar e enviar.

### Erros meus na sessão
- **Heredoc comendo barra invertida — três vezes.** Quebrou o dicionário de
  sinais, o patch do smoke e o gerador da versão leve. A correção já estava
  registrada desde 15/09 e eu repeti assim mesmo.
- **Crase dentro de comentário CSS** encerrou o template literal do gerador. O
  `node` falhou e o smoke validou o arquivo ANTIGO, que continuava em disco —
  quase entreguei a versão errada.
- **Guarda de versão do Bootstrap com string literal** — o banner do CSS tem
  dois espaços e o do JS tem um. Barrou o arquivo certo. Falhou alto, que é o
  que uma guarda deve fazer.
- **Prova do smoke virou falso positivo**: ela conferia que `container-xxl` não
  aparecia no arquivo, e o CSS do Bootstrap DEFINE essa classe.
- **Título de grupo a 13,5px** — igual à fonte da tabela, indistinguível de um
  sinal em negrito. Só apareceu medindo `getComputedStyle` no navegador.

### Provas novas
- **Ordem crescente por efeito** nas duas telas — e **testei o teste**: gerei
  uma versão com a ordem invertida de propósito e ele acusou os quatro cartões.
- **Ordem fixa dos grupos**, conferida também depois de trocar de base.
- **Nada buscado na rede** — `performance.getEntriesByType('resource')` vazio.
- **Colunas centralizadas**, com as duas exceções (título de grupo e glossário).
- **Crase em comentário do CSS** — a que quebrou o gerador.

### Próximos passos
- [ ] Revisar e enviar o `email-precificacao.md` (destinatários em aberto)
- [ ] 🔴 Declarar o objetivo do acesso à Dealers — segue sem pasta em `subjects/`
- [ ] Gerar o `schema.md` do `information_schema`, agora que há conexão — o
      import à mão já nasceu faltando 44 tabelas
- [ ] Levar o VMV-sentinela da Dealers a quem cuida daquela operação
- [ ] Modelo multivariado, só com o que se sabe ANTES da venda

### Notas
- **A rede caiu no meio da sessão** e os dois bancos pararam de responder ao
  mesmo tempo — é a VPN, não host. Voltou depois. O acesso direto depende dela.
- O acesso ao GitHub também esteve indisponível; o commit ficou represado desde
  17/09 e saiu só agora, com 2 dias de trabalho junto.
- Credenciais: `bi_read_cars2you` e `bi_read_wl_dlc_prd`, as duas somente
  leitura, MySQL 8.4.8 nos dois lados.

---

## 🌅 Manhã — reorganização do cérebro

### Resumo
A pedido do Thomas, os seis ajustes que eu tinha identificado no modelo do
brain, mais um espaço dedicado ao conhecimento de banco **organizado por
projeto**. O objetivo: responder pedido sem reabrir o banco.

### O que foi feito

**Espaço dedicado, em dois níveis** — `context/banco-de-dados/`:

| nível | arquivos |
|---|---|
| `plataforma/` | `schema.md`, `schema-gerado.md` 🆕, `dominios.md`, `acessos.md` 🆕, `qualidade.md` 🆕, `receitas.md` 🆕, `tabelas-nao-documentadas.md`, `colunas-ocultas.md` |
| `projetos/precificacao/` | `definicoes.md` 🆕, `indicadores.md` 🆕, `palavras-chave.md` 🆕 |
| `projetos/c6/`, `projetos/rel-veiculos/` | stubs honestos, dizendo onde o conhecimento está hoje |

O `README.md` virou índice por pergunta: "que tabela guarda X?", "esse campo é
confiável?", "quanto deu tal número?" — cada uma aponta para um arquivo.

**Os seis ajustes:**
1. ✅ `mapa.md` ganhou `automations/bancos/`, `automations/precificacao/` e
   `memory/decisions/`, que estavam fora — e a regra de pasta nova.
2. ✅ **Fonte única para número medido.** `indicadores.md` por projeto, com data
   e método. Regra escrita no `mapa.md` e no `/salve`.
3. ✅ `/salve` passa a **commitar local mesmo se o push falhar**, e avisar.
4. ✅ Sessão que atravessa a meia-noite ganha um arquivo por data.
5. ✅ **`gera-schema.py`** — o schema sai do `information_schema`, com as 193
   tabelas. Omite 79 nomes de coluna sensíveis por política.
6. ✅ `automations/dealers-db/` → `automations/bancos/` (acesso) +
   `automations/precificacao/` (estudo). O nome antigo mentia.

### Decisões tomadas
- **`schema-gerado.md` fica AO LADO do `schema.md`, não por cima.** O gerado é a
  verdade sobre estrutura; o escrito à mão é a verdade sobre significado —
  descrição em português, agrupamento por domínio. Unificar exige preservar as
  descrições, e isso é trabalho humano.
- **O gerador omite nome de coluna sensível.** O nome não é PII, mas listar
  `last_owner_document` ao lado de `last_owner_name` é um mapa de onde procurar.
  São contados, não nomeados: 79.
- **Stubs honestos** para `c6` e `rel-veiculos` em vez de mover conhecimento que
  eu não medi. Cada um diz onde o conteúdo está hoje.

### Erro meu, e a correção que ficou
Troquei as regras do `.gitignore` por regras **sem caminho**, para sobreviverem
a renomeação. Era pior: `analitico-*.json` sem caminho também pega os arquivos
**já commitados** do estudo da Cars2You, e o commit os **apagaria**. Peguei
conferindo `git diff --cached --name-status | grep "^D"` — três arquivos iam
embora.

Voltei ao caminho e **a proteção de verdade virou outra coisa**: uma guarda de
tamanho no `/salve`, que para o commit se houver arquivo acima de 1 MB no
stage, e a checagem do que está sendo apagado. Essa não depende de o caminho
estar certo.

### Números que passaram a ter fonte única
Cars2You: 118.669 veículos, 47.415 vendas, 193 tabelas, 2.075 colunas.
Dealers: 54.070 veículos, 21.405 vendas, 193 tabelas, 2.074 colunas.
Todos em `projetos/precificacao/indicadores.md`, medidos em 18/09.

### Próximos passos
- [ ] Preencher `projetos/c6/` e `projetos/rel-veiculos/` — medindo, não copiando
- [ ] Decidir se `schema.md` e `schema-gerado.md` se fundem, e quem preserva as
      descrições
- [ ] Os pendentes de ontem seguem: e-mail de precificação, objetivo do acesso à
      Dealers, VMV-sentinela, modelo multivariado

---

## 🌙 Noite (~17:00 - 00:10) — Radar de Estoque: duas regras novas, e um erro meu na publicação

> O `/salve` rodou já depois da meia-noite, em 19/09. O trabalho é todo de 18/09,
> então fica aqui — não abri arquivo novo para uma madrugada sem trabalho.

### Resumo
Mudança de eixo do Radar de Estoque, pedida com validação de impacto **antes** de
executar: a janela de eventos passou a olhar **para a frente** (7 dias) em vez de
para trás, e a correspondência mínima foi **desligada**. Medi as duas sobre o run
50406 antes de mexer numa linha. Rodou em produção (execução **52212**) e publicou
— **na pasta errada, por erro meu**.

### Decisões tomadas
- **Janela = eventos que encerram nos próximos 7 dias.** `PISO_FIXO = ''`,
  `HORAS_ADIANTE = 168`, `TETO_FIM_DO_DIA = true`. Piso à meia-noite de hoje, teto
  às 23:59:59 do sétimo dia.
- **Sem filtro de `e.status`.** Não remove nada que a janela já não remova (zero
  eventos com `status != 1` têm fim no futuro) e o status **atrasa** — nove eventos
  do 50406 ainda estavam com `status = 1` horas depois de encerrados.
- **Os dois lados do recorte fecham em limite de DIA.** Piso no relógio custaria 51%
  da base (625 de 1.221 veículos, no 50406); teto no relógio faria evento do sétimo
  dia entrar ou não conforme a hora do run.
- **`CORRESP_MIN = 0`** — o piso passa a ser do usuário, na barra deslizante do
  extrato, que nasce em 70%.
- **A prova acompanha a regra, não o número.** As assertivas leem `corresp_min` e
  `TETO_FIM_DO_DIA` da fonte e cobram o comportamento dos **dois** modos. Cravar o
  valor faria a prova falhar por mudança de ideia — e deixaria de cobrar no modo novo.

### O que a medição mostrou antes de executar
Sobre o run 50406 (1.221 veículos, 33.503 pares):

| | antes | com a janela de 7 dias |
|---|---|---|
| eventos | 50 | 18 |
| veículos | 1.221 | 493 |
| pares | 33.503 | 13.195 |

Saíram 726 veículos de evento encerrado, **723 deles sobra** — o eixo do relatório
mudou, não é a mesma base menor.

🔴 **Desligar o mínimo mexe quase nada, e a medição evitou a surpresa:** ele zerava
só **10 veículos de 1.221**; os pares iriam de 33.503 para no máximo 36.409 (+8,7%).
Quem corta é o **`TETO_LOJAS = 30`** — 110.006 pares por run. Se o pedido um dia for
"ver mais loja por veículo", a alavanca é o teto.

### Defeito achado de passagem
`q_eventos` rodava com **uma página só** e o teto de 50 linhas do MCP truncava
calado. Na janela aberta havia 51+ eventos: o **21746** tinha veículo no relatório e
não estava na lista — sumia do filtro de evento da página. Corrigido: `q_ev_total`
virou gabarito, `q_eventos` ganhou 4 páginas, e a fase 2 mata o run se faltar página.

### 🔴 Erro meu: a publicação foi para a pasta antiga
Eu disse que `Virar Arquivo` e `Subir no SharePoint` "estão como já estavam". **Não
estavam** — `PASTA`/`PREFIXO` mudaram na sessão anterior e moram justamente no
`virar-arquivo.js`. O Thomas transcreveu os 3 nós que eu listei; a lista é que estava
errada. A execução 52212 publicou em
`Relatórios Aderência Veículos/relatório-aderência-veículos-2026-09-18.html` —
acrescentando ao acervo antigo em vez de encerrá-lo.

Lição: **a lista de nós a transcrever tem que sair do `git diff`, não da memória.** O
`_confere_transcricao.py` teria pego — ele compara os quatro nós byte a byte.

### Execução 52212 (18/09, 11min39s, sucesso)
| | |
|---|---|
| janela | `2026-09-18 00:00:00` → `2026-09-25 23:59:59` ✅ |
| eventos | **25** · veículos **1.096** · pares **32.880** |
| sem par | **0** · cortados pelo mínimo **0** |
| cortados pelo teto | **986.559** |
| HTML | 4,89 MB |

Minha projeção de volume (~490 veículos) errou: foi feita sobre uma sexta olhando um
fim de semana; hoje a janela pega 25 eventos, incluindo os feirões LM de 21/09 e o
RZK AGRO. Base maior, regra certa.

✅ **Duas confirmações de que as decisões foram acertadas:** 6 dos 25 eventos estão
com `ev_status = 0` e encerraram hoje entre 14h e 16h (69 veículos) — o piso à
meia-noite os manteve, e o filtro de status os teria descartado.

### Ferramentas novas
- `_extrai_do_html.js` — tira o `DADOS` de dentro de um `saida-*.html`. Existe porque
  o `_extrai_execucao.py` depende dos dumps do MCP, e a sessão passou horas sem
  conector de n8n. Validado contra o 50406: pares, veículos e resumo idênticos.
- `_confere_transcricao.py` passou a aceitar o JSON cru do **Download** da interface,
  não só o formato do MCP — sem isso, quem não tem o conector não consegue conferir,
  que é justamente quando a transcrição é manual.

### Armadilha de Windows, medida
`Get-Content` sem `-Encoding UTF8` lê o arquivo como ANSI: "veículos" vira
"veÃ­culos". Não dá erro — o nó roda e o relatório sai com o glossário inteiro
corrompido. Testado nesta máquina.

### Conectores
Passei metade da sessão sem n8n. O Microsoft 365 conectou mas está **bloqueado por
Conditional Access** (`AADSTS53003`, trace `bc8a49e4-af71-4da1-93d3-c1bf86cc5900`) —
um admin do tenant acha isso nos logs de entrada não interativos. Conectou também um
**Dealersclub - Database** que eu não usei: acesso novo não é autorização, e o
objetivo do acesso à Dealers segue sem ser declarado.

### Pendências
- [ ] **Transcrever o `Virar Arquivo`** e rodar de novo, para a pasta `Radar de
      Estoque` nascer (prazo: 2026-09-21, o Thomas fará segunda)
- [ ] Apagar as **duas pastas antigas** + o arquivo de 18/09 que caiu na #2
- [ ] Reabrir o `TETO_LOJAS`: 986 mil pares ficaram fora dele

### Arquivos modificados
- `automations/n8n-sdk/rel-veiculos/` — `montar-fase1.js`, `montar-fase2.js`,
  `montar-html.js`, `prova-local.js`, `_confere_transcricao.py`, `README.md`
- **novo:** `_extrai_do_html.js`
- `mapa.md` e `context/banco-de-dados/projetos/radar-de-estoque/README.md` — conserto
  de estrago da renomeação de ontem: uma linha de tabela quebrada e um caminho de
  arquivo que virou "n8n-sdk/Radar de Estoque/"

### Notas
Provas ao fim: prova-local (221) · _prova-modelo (46) · _prova_virar_arquivo (18) ·
design (126) · smoke DOM. Todas passando.

Em 21/09 a janela vira `2026-09-21 00:00:00` → `2026-09-28 23:59:59` — os seis
eventos de 18/09 saem e a base será outra. Não estranhar o número não bater.
