# Sessão — 2026-09-04

> Continuação direta da sessão de 03/09 — virou a meia-noite no meio do trabalho.
> O contexto completo (import do protótipo, os quatro lotes, os erros de método) está em
> [`2026-09-03.md`](2026-09-03.md), seção "Noite/madrugada".

## 🌙 Madrugada (00h00 – 03h20) — arquitetura B e o filtro de safra

### Resumo
Fechada a parte do port que dá pra fechar. O relatório de base de clientes roda **no n8n**
sobre a plataforma inteira (**29.010 clientes**, era 2.921 só do C6) com **filtro de safra
Ano/Mês funcionando**. O seletor de whitelabel ficou de fora: a query que o alimenta
estourou o deadline e foi revertida.

### Decisão grande: arquitetura B
Descoberto que o `Montar HTML` (87 KB) **não pode ser editado pela API do MCP** — o
`update_workflow` só aceita o `jsCode` inline, e esse literal sozinho passa de 50 mil
tokens. Nem eu nem um subagente conseguimos transmitir.

Em vez de desistir ou pedir cola manual, os nós reais foram **renomeados** e adaptadores
pequenos assumiram os nomes antigos:

```
Trigger → Montar Queries Base → MCP Exec → Montar Queries → MCP Run → Montar HTML → Injetar Filtros → Anexar HTML
            (24 KB, real)       (real)      (adaptador)   (adaptador)   (INTOCADO)      (novo)
```

O `Montar HTML` lê `$('Montar Queries')` e `$('MCP Run')` **pelo nome**, então recebe
exatamente o que sempre esperou — e segue byte-idêntico à produção (md5 `9e570f948b63`).
Mudança de formato de dado vive nos adaptadores; mudança de interface, no injetor.
Consequência: **o gerador nunca mais precisa ser retransmitido.**

Validada na execução **48480** (13min21s, 9 nós).

### O que ficou de fora, e por quê
`coorte_wl` estourou o deadline na execução 48476 (67s) — **quarto timeout da noite**.
Agrupar por whitelabel sobre a tabela derivada `BEXT` obriga a materializar 29 mil
usuários sem índice; a coorte simples escapa porque agrupa direto por `ym`, sem join novo.
`Montar Queries Base` revertido pro lote 1d.

### Pendências
- [ ] 🔥 **Consertar a `coorte_wl`** — única coisa entre o estado atual e o filtro por WL.
      Hipótese: paginar por whitelabel em vez de `OFFSET`. Complicação conhecida: o WL 7
      sozinho tem 78 meses e estoura o teto de 50 linhas de uma fatia por WL.
      **Medir antes de subir** — dois dos quatro timeouts vieram de hipótese virada conclusão.
- [ ] **Nível 2**: whitelabel filtrando *todas* as seções e abas — 18 queries por WL +
      `rx_buyer_wl`, uma `renderX()` por seção, Chart.js recriado a cada troca. Frente própria.
- [ ] **Distribuição**: o HTML foi a **1,27 MB**. Anexo diário desse tamanho é arriscado —
      alguns Outlook corporativos barram. SharePoint + link já existe nos flows do IGA.
- [ ] Cosmético: `Injetar Filtros` em `[1120,0]`, sobreposto ao nó de e-mail no canvas.

### Arquivos
`automations/n8n-sdk/` — `no-adaptador-queries.js`, `no-adaptador-mcp.js`,
`no-injetar-filtros.js`, `montar-queries-LOTE1e-wl.js`, `gera_lote1e.py`,
`testa_cadeia_1e.js`. Editados: `estado-atual.md`, `subjects/c6/03-historico.md`.

### Notas
- **Placar honesto:** camada de dados em 100% da base ✅ · filtro de safra ✅ · arquitetura
  que destrava edições futuras ✅ · **filtro de whitelabel ❌**.
- **Testar o gerador localmente** (`testa_cadeia_B.js` / `testa_cadeia_1e.js`, alimentados
  com dados gravados de uma execução) foi o que mais economizou tempo: iteração instantânea
  em vez de 13 min de produção por tentativa.
- ⚠️ **"Aluguel" continua sem fonte.** A ata de 02/09 pede "quantos já alugaram"; isso não
  existe no workflow, na skill do Gui nem no protótipo. Se a pergunta vier na reunião de
  hoje, ninguém no material tem a resposta.

---

## 🌅 Madrugada, parte 2 (04h15 – 05h00) — o seletor de whitelabel funcionando

### Resumo
Consertada a `coorte_wl` e **o filtro de whitelabel passou a funcionar**. Execução **48492**
(13min32s): dropdown com os **58 whitelabels**, 516 linhas de dado por WL, e o recorte do
WL43 reproduzindo produção (**2.922 / 240**). Falta estender o filtro às 9 seções que ainda
mostram o total da plataforma.

### A correção — e por que ela vale mais que si mesma
A versão anterior (lote 1e) usava `LIMIT/OFFSET` sobre o universo inteiro: cada uma das 16
páginas recalculava os 29.010 usuários do `BEXT`, com dois `EXISTS` cada, pra descartar 50
linhas. Estourou o deadline aos 67s.

**Lote 1f fatia por ANO DE CADASTRO.** O recorte entra como `u.created_at >= 'YYYY-01-01'`
**dentro do `BEXT`**, em coluna indexada — cada consulta monta um `BEXT` de ~1/7. 24 consultas
(8 anos × 3 páginas).

Escolhido **por medição** (`mede_shards.py`), não por hipótese:

| Estratégia | Maior fatia | Veredito |
|---|---|---|
| Faixa de `whitelabel_id` | 304 de 516 linhas (59%) | WLs grandes se concentram nos ids baixos; e o filtro só age **depois** do `BEXT` |
| **Ano de cadastro** | 92 linhas / 6.797 usuários (23%) | ✅ equilibrado, e filtra onde custa |

**Este é o padrão pro nível 2.** As 18 queries restantes têm a mesma forma (`BASE` como
subquery + agregação) e o mesmo risco de estourar ao ganhar `GROUP BY` por whitelabel.
Agora existe precedente medido — não precisa descobrir o teto a 14 min por execução.

### Estado da entrega
**Funciona:** 18 valores filtráveis por whitelabel **e** por safra — 8 KPIs, 6 cards de
Situação, 4 etapas do Funil. Tudo rodando no n8n, com o `Montar HTML` intocado.

| Recorte | Base | Compradores | Volume |
|---|---|---|---|
| Todos | 29.010 | 2.109 | R$ 2.506.019.431 |
| 43 — C6 Auto | 2.922 | 240 | R$ 146.614.710 |
| 7 — Marketplace | 14.746 | 1.450 | R$ 1.959.662.502 |
| 63 — Bradesco | 2 | 0 | R$ 0 |

**Não funciona ainda:** Evolução (9 gráficos), UF, os 4 Top 10, Destaques, Recência e as duas
abas de Raio-X — seguem no total da plataforma. Correto, só não filtrado.

### Próximos passos (nível 2, ordem de menor risco)
1. **Recência** — 1 query. O protótipo já tem o molde de `renderRec()`.
2. **UF** — 2 queries, tabela + 1 gráfico.
3. **Top 10 (×4)** — 4 queries. Exige `ROW_NUMBER() OVER (PARTITION BY wl)`; `LIMIT` não dá
   top-10 dentro de cada whitelabel.
4. **Novos vs recorrentes** — 3 queries, 3 gráficos.
5. **Evolução** — 7 queries, **9 gráficos Chart.js** destruídos e recriados a cada troca.
   A mais pesada; deixar por último.
6. **Raio-X** — 1 query (`rx_buyer_wl`). Mais barato do que parece: os arrays `RX_*` já são
   indexados por `buyer_user_id`, então basta o mapeamento comprador→whitelabel.
7. **Destaques** — zero query; deriva do que estiver embarcado.

Aplicar em todas o padrão do lote 1f: recorte por ano **dentro** da subquery.

### Arquivos
`automations/n8n-sdk/montar-queries-LOTE1f-wl-por-ano.js` (no ar, md5 `8fe100bb`),
`gera_lote1f.py`, `mede_shards.py`.

### Notas
- Cinco timeouts no total nesta jornada. Os três primeiros foram informativos; **dois foram
  erro meu de método** — hipótese tratada como conclusão. O lote 1f foi o primeiro em que eu
  medi antes de subir, e passou de primeira.
- ⚠️ **"Aluguel" continua sem fonte** — segue sendo o único item da ata sem resposta em
  artefato nenhum.
- O HTML está em **1,37 MB**. A decisão de distribuição (anexo vs SharePoint + link) ficou
  em aberto e vira problema no primeiro envio diário.

---

## 🌇 Dia inteiro (10h30 – 23h20) — o lote 2 e o gerador novo

### Resumo
Construído o **lote 2**: filtro de whitelabel alcançando a página inteira, e um
**gerador de HTML novo, escrito do zero**, reativo por desenho. Tudo provado
localmente. A execução final foi cancelada pelo Thomas após **mais de 1 hora** —
e a autópsia achou a causa principal: **74% das chamadas eram para o Raio-X, que
tinha sido ocultado da tela**.

### O que ficou pronto e provado
- **15 queries `_wl`** (`_lote2_bloco.js` + `gera_lote2.py`, 9 asserts).
  27 queries globais seguem **byte-a-byte** intactas — provado por `prova_lote2.js`.
- **Gerador novo em 4 partes** (`gerador/`), montado por `build_gerador.py` com 6 provas.
  Layout, títulos, cores e os 12 gráficos **extraídos do relatório anterior**, não
  reinventados. Nó principal em **27 KB** contra os 87 KB de produção.
- **Testado em navegador de verdade**: 21 de 24 medidas reagem ao filtro de whitelabel.
  As 3 que não reagem são as do lado da oferta, corretas por desenho.
- Recorte nos **6 whitelabels** pedidos: 43, 48, 62, 65, 7, 4 — aplicado no SQL.

### Decisões tomadas
- **Gerador novo do zero** em vez de estender o injetor. Decisão do Thomas; eu
  recomendei reaproveitar e registrei a ressalva. Deu certo: 27 KB, e o injetor
  por texto não escalaria pra 12 gráficos.
- **Recorte de whitelabel no SQL, não no cliente.** "Todos" tem de ser a UNIÃO dos
  6 — somar os recortes contaria duas vezes quem está em dois whitelabels.
- **HTML montado localmente**, não no n8n. Transmitir app (46 KB) + esqueleto
  (27 KB) pela API custa mais que baixar o `DADOS` (~350 KB) e montar fora.
- **4 rankings por whitelabel fora da fila** — medido, não suposto: estouram o
  deadline mesmo reescritos. O HTML cai no ranking global com selo.
- **Abas de Raio-X ocultas** por flag (`MOSTRAR_RAIOX`), nada apagado.

### O que as sondas ensinaram (workflow `7TCmS8JFacDTmySQ`)
| Execução | Lição |
|---|---|
| 48571 | 9 das 15 `_wl` passam. `uf_compra_wl` estoura. **`uf_cadastro_wl` PASSOU** — materializar a base por WL não é o gargalo |
| 48573 | Fatiar por ano da compra **não** resolveu — hipótese minha tratada como conclusão |
| 48644 + 48645 | **Idênticas**: não é carga, é a FORMA. Pré-agregar antes de encostar em `user_whitelabels` passa; juntar direto contra as negociações estoura em qualquer variação |

### 🔴 A execução 48693 e o que eu errei
Rodou 1h05 e foi cancelada. `runData` vazio — nada aproveitável. Duas causas, ambas minhas:

1. **74% da carga era desperdício.** Ao ocultar o Raio-X eu escondi na interface e
   **não tirei da fila**: 915 das 1.231 chamadas buscavam dado para tela invisível.
2. **`onError: continuar` transformou falha rápida em moagem.** Cada chamada que
   estoura queima os 15s inteiros do deadline e segue. Sem isso, o run teria morrido
   em minutos me dizendo onde.

Também previ que o recorte dos 6 whitelabels **aceleraria** o run. Foi suposição
apresentada como expectativa, e o resultado não confirma. Só medindo pra saber.

### Próximos passos (na ordem)
- [ ] Tirar as 8 queries `rx_*` da fila — **915 chamadas a menos, custo zero**
- [ ] Desligar `onError: continueRegularOutput` no nó MCP — falhar rápido
- [ ] Rodar `QImk2D4HdzIqHZe9` (~301 chamadas)
- [ ] Baixar o `DADOS` e montar com `node monta_html_de_dados.js <dados.json>`
- [ ] Se ainda estourar: sonda de 8 chamadas comparando `BASE` com e sem o recorte
- [ ] **Tabela de ativação** (pedida hoje, adiada a pedido): queries `ativacao` e
      `ativacao_wl` já escritas; `sonda-ativacao.js` pronta, 15 chamadas

### Arquivos criados
`automations/n8n-sdk/`: `_lote2_bloco.js`, `gera_lote2.py`, `prova_lote2.js`,
`montar-queries-LOTE2-full-wl.js`, `build_gerador.py`, `gerador/` (4 partes),
`montar-css.js`, `montar-app.js`, `montar-dados.js`, `montar-html-lote2.js`,
`testa_gerador_local.js`, `monta_html_de_dados.js`, `sonda-*.js`

### Notas
- ⚠️ **Bug no relatório de PRODUÇÃO**, achado ao comparar: as 12 barras de recência
  saem com `width:30,4%` — vírgula decimal é CSS inválido, o navegador descarta.
  **As barras estão zeradas em produção hoje.** O gerador novo emite com ponto.
- ⚠️ `saida-teste-local.html` **não é compartilhável**: só `coorte` e `coorteWl` são
  reais; as outras 32 fontes são sintéticas, incluindo nomes de loja e CNPJs.
- O filtro de ano/mês significa coisas diferentes por seção — safra de cadastro na
  coorte, período do evento nas séries mensais, e nada onde não há eixo de tempo.
  Cada grupo tem selo próprio dizendo qual.
