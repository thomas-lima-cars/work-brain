# Sessão — 2026-09-23

## 🌆 Tarde e noite (até ~20h) — Radar de Estoque

### Resumo

Dia inteiro no Radar de Estoque. Entraram o farol de oferta (VMV) e o filtro dele. A tabela
de veículos foi enxugada e a barra de score ganhou cor por valor. Tentei duas fusões de
consulta para cortar chamadas: uma ficou e a outra derrubou o run 53385, e foi revertida.
Com a VPN de volta, medi o banco direto e achei o gargalo real (`access_logs`). No fim, a
**trava de grupo de cliente** entrou no cruzamento e rodou limpa no run **53433**.

### Decisões tomadas

- **Farol de oferta por veículo:** vermelho = sem oferta; amarelo = com oferta sem atingir
  o VMV (inclui VMV não declarado); verde = VMV atingido (`>=`). O amarelo é `#E3A008`
  literal, porque o token `--atencao` é vinho.
- **Etiqueta "Sem Ofertas" saiu da linha do veículo** (o farol vermelho já diz). As outras
  etiquetas de sobra ficam.
- **Glossário e avisos só com regra vigente.** Mudança de regra não aparece em nenhum dos
  dois; aviso é só sobre o dado do run (ex.: veículo sem loja). Registrado em
  `design/regras-de-layout.md`.
- **Filtro de farol** entre os filtros. **Coluna "Lojas" saiu** da tabela; o veículo com
  link vai em cima e UF + evento embaixo. **Barra de score com UMA cor por valor**: azul
  claro até 25%, azul escuro a partir de 75%, interpolado no meio.
- **Fusão de consultas:** `q_contato` + `q_cluster` dentro de `q_lojas` **ficou**. A fusão de
  `q_modelo` + `q_categoria` em `q_moda` **estourou o prazo do MCP nas 27 páginas** (run
  53385) e foi revertida no mesmo dia.
- **Direção do Thomas para o banco:** consultas diretas e simples; o processamento fica no
  n8n. Nada de novas fusões.
- **Trava de grupo de cliente**, além do whitelabel:
  - a loja herda o grupo de **todos** os usuários
  - **todo grupo listado dá acesso**, inclusive os de nome "Bloqueio"
  - grupo **inativo** não dá acesso
  - evento sem grupo fica só com a trava de canal
- **Sempre registrar o significado de tabela descoberto por medição** na documentação do
  banco, sem perguntar. **Sempre guardar o export do n8n** depois de validar
  (`workflow-n8n-*.json`, fora do git) e tirar dos Downloads.

### Medições que valem guardar (fontes únicas)

- Significados: `event_shops` é o **dono do evento** (vendedor), não trava de compradora;
  grupo mora no usuário; `access_logs` sem índice (shop_id, created_at) →
  [`dominios.md`](../../context/banco-de-dados/plataforma/dominios.md).
- Run 53433 (tempos por nó, pares cortados, custo da trava) →
  [`indicadores.md`](../../context/banco-de-dados/projetos/radar-de-estoque/indicadores.md).

### Pendências

- [ ] **Decidir: tirar `ult_oferta`/`ult_acesso` de dentro da `q_lojas`?** Medido: como
      subconsulta correlacionada custa ~30s por passada; como consulta direta, ~19s. O
      `access_logs` sozinho custa ~14s e se repete nas 27 páginas. Proposta feita, sem
      resposta.
- [ ] `build_wf.py` para numa guarda antiga ("sem cron") que confunde "cronológica" com
      cron. O `.wf.ts` é gravado antes, então está atualizado; a guarda precisa de ajuste.
- [ ] **Dado pessoal no histórico do git:** e-mail e telefone reais de um contato de loja
      num comentário do `montar-html.js` e no README. Tirei dos arquivos hoje, mas os
      commits antigos ainda têm.
- [ ] 1 veículo veio duplicado do banco no run 53433 (descartado e declarado). A
      `q_veiculos` não mudou; investigar se se repete.

### Próximos passos

- [ ] Conferir o relatório publicado do run 53433 no SharePoint (evento 23995 deve mostrar
      só 1 loja)
- [ ] Responder a pendência do `ult_acesso`

### Arquivos modificados

- `automations/n8n-sdk/rel-veiculos/`: `montar-fase1.js`, `montar-fase2.js`,
  `montar-html.js`, `prova-local.js`, `_smoke_dom.js`, `rel-veiculos.wf.ts`, `README.md`
- `context/banco-de-dados/plataforma/dominios.md`
- `context/banco-de-dados/projetos/radar-de-estoque/indicadores.md` (novo) e `README.md`
- `design/regras-de-layout.md` · `.gitignore` · `estado-atual.md` ·
  `subjects/cars2you/03-historico.md`

### Notas

- 🧪 **"Afirmar antes de medir" de novo:** estimei 10 a 13 minutos para o MCP Fase 2 a partir
  do run 53272. Os dois runs do dia passaram de 15 minutos: o banco está a ~4,5s por
  chamada, contra 2,9s naquele run.
- O conector de banco que aparece nesta sessão (`e34110a1…`) **não é o cars2you**: lista
  tabelas de logística (`removals`, `dispatch`, `advisories`). O caminho documentado é
  `automations/bancos/consulta.py`.
- O disparo do workflow pelo MCP foi barrado pelo modo automático (ele publica no
  SharePoint). O Thomas disparou pela interface.
