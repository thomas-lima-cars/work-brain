# Sessão — 2026-09-03

## 🛠️ Onboarding (sessão de setup)

### Resumo
Construção do work-brain do zero, a partir do briefing recebido em
`Downloads/second brain - cerebro - briefing - work - brain - thomas.md`.
Criada a árvore completa (context, subjects, memory, inbox), as 4 skills, os templates
de frente, o `mapa.md` e o `estado-atual.md`. Repo conectado ao GitHub privado e as 6
frentes reais abertas e classificadas. Nenhuma frente foi trabalhada — sessão de setup.

### Decisões tomadas
- **Repo em `C:\Users\thoma\Documents\work-brain`**, fora da pasta `Documents\Claude`.
- **Campos de contexto não essenciais marcados `NULL`** de propósito (modelo de negócio,
  estrutura de times, rituais, reporte, expectativas, stakeholders). NULL significa
  "vazio deliberado" — o modo onboarding do `/cerebro` não deve cobrar esses campos.
- **Jira fora.** Removida a seção 2 e o aging por status da `/rotina`, como o briefing
  manda nesse caso. O aging foi reescrito para usar só o estado do brain (pendência que
  reaparece em 2+ sessões, frente sem histórico há >7 dias, "esperando outros" há >5 dias).
- **Cargo: Product Owner.** O briefing se contradizia (PO no topo, "BI" numa tabela);
  vale a primeira frase, que é explícita. O "(BI)" era resíduo do brain original do Caio.
- **Frentes definidas pelo Thomas**, não pelo briefing: `cars2you`, `bradesco`, `c6`,
  `itau`, `lm`, `outros`. Quentes: **cars2you** e **bradesco**.
- **`outros` é frente coringa** para o que não se encaixa nas demais.

### Correções ao briefing
- O briefing dizia que **Bradesco havia sido removido** — mas é frente ativa do Thomas.
- O briefing citava **IGA, Win Leilões, Dealers Club, Tuunelis** como frentes dele — não são.
  Registrado em `context/produtos.md` para nenhuma sessão futura ressuscitá-las.
- O briefing supunha uso de Jira — não usa.

### Pendências
- [ ] Descrever o estado atual de `cars2you` e `bradesco` no `estado-atual.md`
      (ficaram como placeholder por falta de informação, não por esquecimento)
- [ ] Esclarecer o que é a sigla **LM** e preencher `subjects/lm/01-contexto.md`
- [ ] Preencher `01-contexto.md` das frentes conforme o trabalho passar por elas

### Próximos passos
- [ ] Primeira sessão de trabalho real: `/cerebro` → `/rotina` → trabalhar → `/salve`
- [ ] Deixar o `/salve` preencher o estado das frentes quentes a partir do que rolar

### Arquivos modificados
Setup completo — 25 arquivos em 7 commits (`27e920a`..`ff20b7a`).
Destaques: `mapa.md`, `estado-atual.md`, `.claude/skills/*/SKILL.md`, `context/*`,
`subjects/{cars2you,bradesco,c6,itau,lm,outros}/*`.

### Notas
- **Percalços do GitHub:** o repo já existia com um `Initial commit` (README) e a
  autenticação não estava concluída. O `Repository not found` era 404 de repo privado
  sem auth, não repo inexistente. Resolvido com `git pull --rebase` (sem force-push);
  o `README.md` do GitHub foi preservado.
- **A autenticação vive no Git Credential Manager, não no `gh`.** O `gh` está instalado
  (`C:\Program Files\GitHub CLI\gh.exe`) mas **não** autenticado — evitar comandos `gh`
  neste repo e usar `git` puro.
- Push do `/salve` e `git pull --rebase` do `/cerebro` testados de ponta a ponta, ambos OK.

---

## 🌆 Tarde — import do contexto n8n

### Resumo
Primeira sessão de trabalho real do brain. Sessão começou no diretório errado
(`Documents\Claude`) — movida pro `work-brain` pra as skills carregarem. Importados três
artefatos que estavam soltos em `Downloads`: a pasta `automations/` (16 arquivos de flows
n8n, scripts e estado), o `n8n-cars2you-contexto.md` e o `auditoria-estoque-loja.wf.ts`.
O import revelou que **três frentes estavam classificadas errado** no onboarding e que
havia um **prazo crítico pra sexta 04/09** que o brain não conhecia.

### Decisões tomadas
- **Nova categoria de frente: "⚙️ Rodando em produção (sem demanda ativa minha)".**
  A taxonomia binária quente/parada não dava conta — `itau` e `lm` têm automação 🟩
  rodando sozinha mas não consomem atenção. Chamar isso de "parada" fazia o `/cerebro`
  esconder a frente; chamar de "quente" poluiria o topo do painel. Propagado pros
  templates das skills `/cerebro` e `/salve`.
- **O md importado foi dividido, não copiado inteiro.** Seções técnicas → `automations/`,
  seção de negócio → `memory/inputs/meetings/`, conforme a convenção do brain.
  Os dois arquivos se linkam.
- **`n8n-sdk/` separado de `n8n-code/`.** Workflow inteiro como código é coisa diferente
  de conteúdo de nó Code isolado.
- **Celular da Thais redigido** de `lista-lm-propostas.md` — a convenção do brain é não
  guardar telefone. IP do Evolution mantido (infra, não PII).
- **`automations/estado/*.json` fica versionado por ora** — vale como snapshot. Se sujar
  o histórico, entra no `.gitignore`.

### Correções ao estado anterior do brain
- **`c6` estava "parada"** — na verdade tem o prazo mais urgente do momento. → quente.
- **`itau` estava "parada"** — são 8 workflows 🟩 em produção. → rodando em produção.
- **`lm` estava "parada"** — estoque com movimento até 25/08. → rodando em produção.
- **`IGA` aparecia como "fora do radar" em `produtos.md`** — IGA **é** a operação Itaú,
  não um terceiro. Era duplicação da própria frente `itau`.
- **`README.md` do `automations/` dizia "esta pasta está vazia de código"** — falso depois
  do import. Reescrito com índice do que existe.
- **`crons.md` estava com tabela vazia** enquanto 9 crons rodavam. Preenchido — mas
  **a partir dos docs, não da API**. Precisa validar.

### Pendências
- [ ] **Relatório C6 em 100% da base com visão de safra** (prazo: 2026-09-04) — com Everton.
      É avaliação de skill: se não sair, a diretoria busca externo.
- [ ] Pulso de Eventos: encurtar mensagem + bolinha colorida — alinhar com Doni
- [ ] Validar `automations/crons.md` contra o n8n ao vivo
- [ ] Higiene n8n: arquivar 3 workflows TEMP do C6 + o "Testes"
- [ ] Confirmar os cargos inferidos em `context/stakeholders.md`
- [ ] Descrever o estado de `bradesco` — única frente quente que segue sem conteúdo
      (nada sobre ela apareceu no contexto n8n)
- [ ] Esclarecer a sigla **LM**

### Decisões em aberto (não minhas ou sem dono)
- Onde hospedar os relatórios (hoje cloud/máquina do Caio)
- Skip/Adapta — Caio, Gui e Daniel avaliam
- Lance Fácil BTB merece `subjects/` própria? Tem 2 whitelabels e 2 workflows
- Canal único de alerta de falha dos crons — hoje são 9 crons sem `onError`

### Próximos passos
- [ ] Amanhã cedo: `/cerebro` → atacar o relatório C6 direto. Abrir `ehsqQo6hiPDRf58I`
      e mapear o gap pra visão de safra.
- [ ] Se sobrar tempo: importar o `.ts` do relatório C6 também, pro `n8n-sdk/`

### Arquivos modificados
**Novos:** `automations/` (18 arquivos, incl. `n8n-ambiente-cars2you.md`,
`n8n-sdk/auditoria-estoque-loja.wf.ts` + README, `crons.md` preenchido),
`memory/inputs/meetings/2026-09-02-squad-de-relatorios.md`.
**Editados:** `mapa.md`, `estado-atual.md`, `context/produtos.md`,
`context/stakeholders.md`, `subjects/{c6,itau,lm,cars2you}/03-historico.md`,
`subjects/c6/{01-contexto,04-cards}.md`, `.claude/skills/{cerebro,salve}/SKILL.md`.

### Notas
- ⚠️ **A cópia `GgjVZlU04wvJzLbK`** (Auditoria de Estoque, projeto pessoal do Thomas)
  está inativa com `MODO='producao'` e gatilho 08h. Ativar sem trocar pra `'teste'`
  dispara e-mail pra lista completa em paralelo ao original.
- **Lição do brain:** "frente parada" no onboarding significava "não sei o que rola nela",
  não "não rola nada". Três de seis frentes estavam classificadas por ausência de
  informação. O import de artefato real é o que corrige isso — vale repetir com o que
  ainda estiver solto em `Downloads`.
- O `wf.ts` da Auditoria teve MD5 conferido contra a origem no import.
- O doc do IGA registra um incidente antigo instrutivo (03/07, evento 23422 disparado com
  78 lotes abertos por erro de fuso no failsafe) — vale reler antes de mexer em cron.

---

## 🌙 Noite/madrugada — port do relatório de base pra multi-whitelabel

### Resumo
Portada a **camada de dados** do relatório "Base de Clientes" de `whitelabel_id = 43`
fixo para a plataforma inteira: **2.921 → 29.010 clientes**, sem contar ninguém duas
vezes. Criado sandbox próprio, quatro lotes de query, duas execuções validadas contra
produção e uma arquitetura nova que contorna o limite de transmissão do MCP.
O filtro de **safra (Ano/Mês)** funciona; o de **whitelabel** ainda não tem dado.

### Decisões tomadas
- **Portar pro n8n em vez de continuar client-side.** Das 11 seções da Visão Geral,
  só 2 eram reativas no protótipo; fazer as outras 9 em JS custava mais que portar.
- **Manter os 58 whitelabels no seletor**, inclusive os vazios (Bradesco tem 2 clientes).
  Decisão do Thomas — sem lista de exceção, sem corte por tamanho.
- **Saída A para os KPIs não-deriváveis:** trazer `total_ofertas` e `status2` de volta em
  vez de mudar a semântica. Eles são lidos toda manhã pelo Caio e pelo Gui.
- **🔥 Arquitetura B** — renomear os nós reais e pôr adaptadores pequenos com os nomes
  antigos, mais um nó injetor depois do gerador. Nasceu de um limite duro: o
  `Montar HTML` de 87 KB **não passa pela API do MCP**. Consequência boa: o gerador
  fica intocado pra sempre e o que muda vive em nós de poucos KB.

### O que está no ar
- Sandbox **`0pUtqToo0zNNibQT`** (projeto pessoal, inativo, e-mail `disabled`)
- `Montar Queries` = **lote 1d** (md5 `3f84b50f39fe50c68d827d9be37ea579`)
- `Montar HTML` intocado (md5 `9e570f948b630938665d9e9486b77b6c` = produção)
- Arquitetura B **testada localmente, ainda NÃO subida**

### Números validados (execução 48437, 13 min 12 s, 876 chamadas)
| | |
|---|---|
| Clientes | 29.010 |
| Com login / Ofertantes / Compradores | 10.958 / 3.924 / 2.108 |
| Nego. (2,3,7) / Vendido (7) / status2 | 45.394 / 43.976 / 1.081 |
| Volume | R$ 2.505.966.430,56 |
| `total_ofertas` | 699.957 |

Invariantes que fecham: `com_login + sem_login = total`; `s1..s6 = total`;
`vendido + status2 <= negociacoes` (sobram 337 = status 3).

### Os três timeouts, e o que cada um ensinou
1. **48410** — `kpi` estourou. Fazia 10 subqueries independentes, cada uma varrendo a
   base. Cabia com 2.921 clientes, não com 29.010. → trocado pela coorte (1 passada).
2. **48429** — `kpi_ofertas`/`kpi_status2` isoladas também estouraram. Eu tinha
   **afirmado** que separar resolveria; era hipótese, não conclusão.
3. Correção: `status2` saiu de graça somando um `COUNT` condicional na varredura que o
   `PC` já fazia; `total_ofertas` trocou `IN` por `INNER JOIN` (BASE devolve 1 linha por
   usuário, então o join não duplica).

### ⚠️ Erro meu que quase foi pro banco
Escrevi as 26 queries do lote inicial a partir dos **fragmentos resumidos do handoff**
em vez de transformar o SQL de produção. Resultado: tabela `vehicle_provider_reports`
que **não existe**, `rx_uf` com join errado (o certo é `shop_stocks`), `rx_faixa` com 4
faixas em vez de 5, `rx_partic` com semântica trocada, `uf_cadastro` fora do padrão.

Pego por revisão antes de subir. Vale como regra: **transformar o texto original com
prova de roundtrip, nunca reescrever de cabeça.**

### Pendências
- [ ] **Nível 1 do lote 2** (próximo passo imediato): 1 query — coorte por whitelabel,
      516 linhas / 11 páginas, já provada pelo protótipo. Destrava o seletor de WL na
      Visão Geral. Custo: +2% de chamadas, payload 14 → 90 KB.
- [ ] **Subir a arquitetura B**: renomear 2 nós, criar 3, refazer conexões.
- [ ] **Nível 2 do lote 2** (frente própria): 18 queries por WL + `rx_buyer_wl`, mais
      uma `renderX()` por seção e Chart.js recriado a cada troca. ~+370 chamadas.
- [ ] Pedir ao Gui/Everton o diretório **completo** da skill — vieram só as regras,
      faltam as queries BC-01..BC-18 e o template HTML.
- [ ] `total_ofertas` pode sair do lote 1d: **nenhum trecho do HTML o consome**.

### Arquivos
**Novos:** `automations/n8n-sdk/` (12 arquivos + `nodes-originais/`),
`automations/fontes-externas/`, `automations/prototipo-safra/`.
**Editados:** `estado-atual.md`, `subjects/c6/{03-historico,04-cards}.md`,
`automations/README.md`, `automations/n8n-sdk/README.md`.

### Notas
- **`HANDOFF.md` do Everton se perdeu** — lido nesta sessão, sumiu de `Downloads/`
  antes do import. Não reconstruído de memória de propósito. O que ele tinha de único:
  o padrão **swap-run-restore** (trocar o `Montar Queries` por sonda, rodar, restaurar —
  o erro no `Anexar HTML` é esperado), o inventário dos 20 workflows, e o alerta dos 6
  destinatários reais na cópia `ZIwusfx9IK1Owpg1`.
- **As 11 execuções com erro daquela cópia não eram defeito** — era o swap-run-restore.
  Corrige a leitura que eu tinha feito no dia anterior.
- **Testar gerador localmente** (`testa_cadeia_B.js`, alimentado com dados gravados de
  uma execução) foi o que mais economizou tempo: iteração instantânea em vez de 13 min.
- ⚠️ O prazo do relatório C6 **é hoje, 04/09**.
